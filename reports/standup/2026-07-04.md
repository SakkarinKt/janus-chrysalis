# Stand-up — 2026-07-04 (run 1)

## Done
- Seeded the literature map (`notes/lit-map.md`) and added 4 Archivist-format paper notes covering all four seed methods named in `loop/GOAL.md`: `notes/papers/mamba-2022.md`, `notes/papers/mabl-2024.md`, `notes/papers/codreamer-2024.md`, `notes/papers/marie-2024.md`.
- No previous stand-up PRs existed to process (this is the first loop run), so the increment fell through to priority 2.

## Learned
- **[high, self_checked]** The four named seed papers occupy four distinct points on a "shared vs. per-agent world model" spectrum, not one: MAMBA (per-agent + comms, PPO update), MABL (per-agent + train-time-only global latent, execution stays fully decentralized), CoDreamer (per-agent + explicit GNN peer-to-peer comms, no central aggregator), MARIE (per-agent Transformer world models + explicit centralized aggregation). This gives a natural 5-point axis for a future controlled ablation (see `notes/lit-map.md`), with "fully independent, no sharing" as the missing 6th/strawman point.
- **[high, self_checked]** MARIE is the only one of the four whose abstract names non-stationarity from co-learning agents as a first-class design target (vs. scalability of centralized world models) — directly matching the mission statement's second axis. It should anchor related-work framing for any future proposal.
- **[medium, self_checked]** A caught-and-corrected error: an early broad search mis-surfaced arXiv 2506.18537 as "MARIE." Re-searching by exact title found that paper is actually MATWM (Deihim et al.), a *later* paper that cites MARIE (arXiv 2406.15836, TMLR) as prior work. Fixed before writing the note — flagging here since it's exactly the kind of mistake `self_checked` tagging exists to catch, and the corrected trail is left in `marie-2024.md`.
- **[low, self_checked]** Preliminary, low-confidence-by-design (n=4 is too small to generalize): none of the four abstracts report directly measuring non-stationarity itself (e.g. world-model prediction error drift as a partner's policy shifts) — they measure it only indirectly via final return/sample efficiency. If this holds as the map grows to 10 papers, it's a candidate gap.

## Blockers
- `WebFetch` returned HTTP 403 for every URL tried this session, including a plain `https://example.com` control fetch with no arXiv or auth involved — looks like a proxy/environment issue rather than arXiv anti-bot behavior specifically. Worked around by relying on `WebSearch` result snippets, cross-checked across ≥2 independent sources per paper (arXiv listing + official GitHub repo / OpenReview / ACM DL / conference-proceedings PDF listing) before writing any claim. All claims are tagged `self_checked` with confidence explicitly capped at `medium` below the abstract/framing level, and each paper note has a "Verification note" section saying exactly what wasn't confirmed. None of today's notes should be treated as `verified` — a primary-source (full-text) pass is needed once WebFetch works again, before any of this is cited in a proposal.

## Decisions needed
- [ ] Is the WebFetch 403 (including on `https://example.com`) a known/expected state of this environment, or should it be treated as a bug to fix before the next research-heavy run? If it's fixable, tomorrow's run would rather spend the increment on a primary-source verification pass than more breadth-first search-only notes.
- [ ] The "Dreaming of Others" paper (arXiv 2605.31361, May 2026, single listed author, factorizes latent into environment/teammate components — very on-topic for the non-stationarity axis) was intentionally *not* added as a full paper note this run because a single-author + very-recent + search-only-verified combination felt like a higher mis-citation risk than the other four. Agree with holding it for a primary-source check, or should it be added now on the strength of the search evidence alone?

## Tomorrow
- If WebFetch is confirmed working: do a primary-source verification pass on today's 4 notes (confirm compute-scale numbers, upgrade confidence tags where the full text supports it) before adding new papers — the lit-map explicitly should not grow past this on unverified foundations.
- If WebFetch is still down: continue breadth — verify and add "Dreaming of Others" (2605.31361), GAWM (2501.10116), and MATWM (Deihim et al., successor to MARIE) via WebSearch cross-checking, prioritizing the non-stationarity/opponent-modeling seed area since it's currently the thinnest-covered of the mission's two named axes.

## Manifest
- level: L1 | increment: 2 (extend literature map) | files touched: 6 | papers added: 4
