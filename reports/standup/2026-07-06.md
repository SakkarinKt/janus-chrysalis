# Stand-up — 2026-07-06 (run 3)

## Done
- Checked PRs #1, #2, #3 for unanswered replies/"Decisions needed" items: none outstanding — PR #1's two decisions were answered and closed out in PR #2's stand-up; PR #2 itself had no open decisions; PR #3 (`chore/oss-hardening`) is a human-authored, already-merged PR on a non-`loop/*` branch, out of scope for this check. No safety valve triggered (need both of the two most recent stand-ups to have *unanswered* items; neither does).
- Extended the literature map (`notes/lit-map.md`) from 8/10 to **10/10 core papers** — target reached. Added two Archivist-format notes: `notes/papers/drama-2024.md` (Drama, arXiv 2410.08893, ICLR 2025 poster — single-agent SSM/Mamba world model, added per priority-4/ADR-0002 relevance, not the sharing-topology axis) and `notes/papers/plans-not-percepts-2025.md` ("Communicating Plans, Not Percepts," arXiv 2508.02912, NeurIPS 2025 workshop — axis point 4, explicit peer-to-peer comms of a compressed self-intention/plan).
- Updated `notes/lit-map.md`'s design-axis description, papers table, and gap observations to reflect both additions, including a new "communication content vs. topology" sub-dimension surfaced by the second paper.
- Ran `npx markdownlint-cli2` locally against all 3 changed/added files (matching the repo's seed CI lint job config) — 0 errors.

## Learned
- **[high, self_checked]** Drama gives the map its first concrete, laptop-scale compute datapoint with a stated parameter count: a **7M-parameter** SSM/Mamba world model, explicitly reported as trainable on a standard laptop, on Atari100k. Cross-checked across 5+ independent secondary sources (ICLR proceedings, OpenReview, emergentmind, aimodels.fyi, paperswithcode) that agree on the ~105% normalized-score figure and the 7M/laptop claims — directly relevant to ADR-0002's SSM-vs-RSSM dynamics-backbone question, and the most laptop-scale-relevant precedent in the map so far (every multi-agent paper here targets SMAC/MAMujoco-class compute with unstated or larger footprints).
- **[medium, self_checked]** "Communicating Plans, Not Percepts" reframes axis point 4 (explicit peer-to-peer comms): CoDreamer shares raw latent state via GNN, this paper shares a compressed *self-intention/plan* generated by imagining one's own future rollout, and "Dreaming of Others" (already in the map) infers a model *of a teammate* from observed behavior with no communication channel at all. These are three structurally different answers to "what gets shared," independent of "who shares with whom" — worth splitting into two axes (topology × content) in the gap-analysis draft rather than treating axis 4 as one bucket.
- **[high, self_checked]** Caught and excluded a paper before it entered the map: "From Pixels to Cooperation: Multi-Agent Reinforcement Learning based on Multimodal World Models" (arXiv 2511.01310) looked like an unusually good fit for the map's still-unfilled axis point 1 ("fully shared" world model) — a shared generative multimodal world model with attention-based fusion across all agents. Before drafting a note, a follow-up search confirmed **the paper was withdrawn by its author** (v2, Nov 2025). Excluded and logged in `lit-map.md` so a future run doesn't re-surface and re-add it without re-checking withdrawal status. Axis point 1 remains unfilled by a clean example — flagged as a specific thing to search for again (with withdrawal-status checking as a standing step) once gap-analysis work resumes literature search.
- **[medium, self_checked]** Drama's Dynamic Frequency-based Sampling (DFS) superficially resembles MATWM's non-stationarity-motivated replay reweighting (both are "smarter replay sampling"), but targets a different failure mode — early-training world-model inaccuracy vs. non-stationarity from co-learning partners. Recorded as a note in both this session's gap observations and Drama's own note so the two don't get conflated into one "replay reweighting" data point in the future gap analysis.

## Blockers
- `WebFetch` re-tested against both new papers' arXiv URLs (`arxiv.org/abs/2410.08893`, `arxiv.org/html/2410.08893`, `arxiv.org/abs/2508.02912`) and still returns HTTP 403 — same persistent autonomous-loop-environment constraint as every prior run. Both new notes are `self_checked` via cross-checked `WebSearch` (≥3 independent secondary sources per paper), confidence capped at `medium` below the framing level, same protocol as the 2026-07-04 seed batch. **Neither should be promoted to `verified` without a primary-source pass** — flagging specifically because the lit map is now feeding directly into gap-analysis work, where unverified numeric claims are higher-stakes than in a pure literature-survey phase.
- "Communicating Plans, Not Percepts" has no recoverable compute-scale or precise quantitative-result figures from search snippets alone (only qualitative "better performance/efficiency/scalability" claims, direction-only) — flagged in its own note as an open item for a primary-source read.

## Decisions needed
- [ ] Lit map hit its 10-paper target this run. Per `loop/GOAL.md` priority 3, the next increment should move to gap-analysis notes / `docs/proposals/` drafts using `TEMPLATE.md`, building on the two-dimensional (sharing topology × non-stationarity mechanism) framing plus the newly-surfaced communication-content sub-axis. Confirm this is the right next step (vs., e.g., a primary-source verification pass on the 5 papers added via search-only in the last two runs, before building proposals on top of them)?
- [ ] Is it acceptable for the gap-analysis draft to reference Drama (single-agent) and "Communicating Plans, Not Percepts" (workshop-tier, no verified quantitative results yet) as design inputs, given both are `self_checked` rather than `verified`? Or should the draft wait for a primary-source pass on these two specifically before citing them for anything beyond qualitative framing?

## Tomorrow
- If the human confirms: begin the first gap-analysis / `docs/proposals/` draft in `docs/proposals/`, using `TEMPLATE.md`, centered on the two-dimensional design space (sharing topology × non-stationarity mechanism) with the communication-content sub-axis noted as a secondary consideration, and the still-unfilled axis point 1 ("fully shared" world model, since "From Pixels to Cooperation" turned out to be withdrawn) flagged as an open question rather than silently dropped.
- Otherwise: primary-source verification pass (if `WebFetch` cooperates) on the 5 most recent search-only additions (GAWM/MATWM/MMSA already got a human-session pass on 2026-07-05; Dreaming of Others also passed; Drama and Communicating Plans, Not Percepts from today still need one).

## Manifest
- level: L1 | increment: 2 (extend literature map to 10/10 target) | files touched: 4 | papers added: 2
