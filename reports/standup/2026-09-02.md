# Stand-up — 2026-09-02 (run 1)

## Done
- Processed PR #62's human reply (posted 2026-09-01T15:29:56Z, on the merge comment) under
  priority 1. The reply resolved PR #61's "Decisions needed" item (the continue/termination-head
  question `reports/standup/2026-09-01.md` raised) with a fourth option — **(A) implement the head
  against the horizon boundary, plus the drift recorded** — and named it "priority 3's first
  sub-piece." Implementing that was this run's one increment:
  - `docs/explainers/0011-continue-termination-head.md`, new: explainer-before-implement, covering
    the target definition, why it's horizon-deterministic scaffolding rather than a genuine
    prediction task in Arm-A's gridworld, and why the post-transition state (not pre-transition) is
    what the head predicts from.
  - `src/model/continueHead.ts`, new: `ContinueHead` — one linear dense layer (`units: 1`) over
    `concat([deterministic, stochastic], 1)`, mirroring `ObservationDecoder`'s shape and
    minimalism.
  - `src/model/losses.ts`: `continueLoss(logit, target)`, new — `tf.losses.sigmoidCrossEntropy`
    directly (confirmed present and differentiable on the pinned `@tensorflow/tfjs-node@4.22.0` by
    calling it, not assumed), so no hand-rolled epsilon floor was needed this time.
  - `src/model/worldModel.ts`: `WorldModel.step()` takes a new required `done: boolean` param
    (`StepResult.done`'s own polarity: target is `0` when `done`, `1` otherwise); `forward()` now
    also calls `continueHead.predict(...)` on the post-transition state and adds `continueLoss(...)`
    to the total; `WorldModelStepResult` and `WorldModelNaNError` both gain a `continueLoss` field,
    and the NaN-halt check (priority 5, `docs/explainers/0009`) now covers it too; a third
    `deriveSeed` salt seeds the head's init; the constructor's warmup pass and `trainableVars` both
    now include it.
  - `src/experiment/freeze.ts`: `runEpisode`'s one production call site now passes `result.done` as
    the new fifth argument — the only call-site change needed anywhere in `src/`/`experiments/`
    (grepped for other direct `WorldModel.step()` callers first; every `experiments/*/run.ts` goes
    through `runEpisode`, none call `.step()` directly).
  - Tests: `test/model/continueHead.test.ts` (new, mirrors `decoder.test.ts`'s three tests);
    `test/model/losses.test.ts` gains three `continueLoss` tests (label/logit polarity, batch-mean,
    gradient flow); `test/model/worldModel.test.ts`: every existing `wm.step(...)` call site updated
    with a `done` argument, the "reconstructionLoss + klLoss equals loss" test extended to include
    `continueLoss`, and two new tests (continueHead train/frozen weight-identity, and a
    both-directions `continueLoss`-descent check under a constant `done`).
  - Verified: `npm test` 154/154 (was 146/146 per PR #62's body — 8 new tests, 0 regressions);
    `npm run typecheck:ratchet` in-repo count unchanged at 44 (baseline 44, exit 0) — this run's
    diff introduced zero new typecheck diagnostics.

## Learned
- `tf.losses.sigmoidCrossEntropy` is present and differentiable on this project's pinned
  `@tensorflow/tfjs-node@4.22.0`, confirmed by calling it directly (`node --input-type=module` smoke
  check) rather than trusting the tfjs-core surface at face value — this codebase has burn history
  with APIs that look present but misbehave under this exact pin (`tf.stopGradient` not existing at
  all despite an ADR recording it as confirmed; `tf.multinomial`'s `seed` param not reliably
  reproducing draws, `reports/quality/2026-08-03-quality-pass.md`'s finding #1). This one held up:
  `self_checked, high confidence`.
- This environment's `npm install` builds `tfjs_binding.node` successfully (unlike the reviewer's
  own sandbox, per PR #62's merge comment: "My sandbox can't build `tfjs_binding.node`... 67/74
  with all 7 failures on that missing addon"). `node_modules/` wasn't present at the start of this
  run — a fresh `npm install` was needed before anything could run. `self_checked, high confidence`
  (direct build + full green suite, this run).
- Confirmed before implementing: no call site outside `src/experiment/freeze.ts` and
  `test/model/worldModel.test.ts` calls `WorldModel.step()` directly (`grep -rn "\.step("` across
  `src/` and `experiments/`) — every `experiments/*/run.ts` goes through `runEpisode`, so the new
  required `done` param needed exactly one production call-site change. `self_checked, high
  confidence`.

## Blockers
- none.

## Decisions needed
- [ ] `PLAN.html:110`'s wording ("Continue/termination head from day one") doesn't distinguish a
  real termination signal from this run's horizon-boundary scaffolding, and `loop/GOAL.md`'s
  boundaries mean I can't edit `PLAN.html` myself. PR #62's review asked for the wording change to
  be raised here for the human to apply. Proposed text: *"Continue/termination head from day one
  (implemented against the fixed-horizon boundary in the absence of a real early-termination
  condition — gradient/shape scaffolding, not a genuine prediction task; see
  docs/explainers/0011)."* Apply as proposed, amend, or leave as-is?

## Assumptions made (proceeding unless told otherwise)
- Treated implementing the continue head as this run's *whole* priority-1 increment (per
  `loop/GOAL.md`: "processing them IS today's increment") rather than also starting a second
  increment afterward — "never work on more than one increment per run" reads this as one, and PR
  #62's review didn't ask for anything beyond this sub-piece plus the `PLAN.html` note above.
- Kept the loss unweighted (coefficient 1 on `continueLoss` in the total, same as `reconstructionLoss`)
  rather than introducing a new `betaContinue`-style config knob — matches `docs/explainers/0006`'s
  precedent for adding a DreamerV3 loss term without inventing a coefficient nothing has asked for
  yet, and nothing in PR #61/#62 asked for one.
- Predicted from the post-transition state (`nextDeterministic`/`nextStochastic`), not the
  pre-transition `prevState` — `done` is a property of the transition that just happened, and this
  mirrors `docs/explainers/0006`'s posterior-vs-prior reasoning for the decoder. Not raised as a
  decision since it follows directly from what `done` already means (`StepResult.done`,
  `src/env/gridworld.ts:65`), not a judgment call with a real alternative.

## Tomorrow
- Pending the human's answer on `PLAN.html:110`'s wording above.
- `reports/quality/2026-08-03-quality-pass.md` currently carries zero unresolved `BLOCKING`
  findings (finding #1 fixed 2026-08-04; #3/#4/#7 remain but are NOTE/NIT) — so priority 2's slot is
  not "fix exactly one" right now; if no new PR reply lands first, the next run should continue down
  `loop/GOAL.md`'s priority list: priority 5's remaining invariant is "WM/AC gradient separation,"
  still not applicable (no actor-critic policy exists yet — `src/agent/policy.ts` has only
  `RandomPolicy`/`QLearningPolicy`, tabular, no shared network with the world model); the other three
  priority-5 items (JSONL telemetry, `manifest.json`, the remaining invariant tests) and priority 6
  (G2 module interface/spec skeletons — replay buffer, λ-returns, spec only) are both live options.

## Manifest
- level: L2-P2-slice | increment: 1 (continue/termination head — priority 3's first sub-piece, per
  PR #62's review, processed under priority 1) | files touched: 8 (3 new: `docs/explainers/0011-
  continue-termination-head.md`, `src/model/continueHead.ts`, `test/model/continueHead.test.ts`; 5
  modified: `src/model/losses.ts`, `src/model/worldModel.ts`, `src/experiment/freeze.ts`,
  `test/model/losses.test.ts`, `test/model/worldModel.test.ts`) | runs: none (code increment, no
  training run)
