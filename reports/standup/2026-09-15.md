# Stand-up — 2026-09-15 (run 1)

## Done

- Checked PR #74 for unanswered human replies first (priority 1): none. Its only human review
  (2026-09-13, `@SakkarinKt`) was already fully answered by the 2026-09-14 run's push
  (`7011e8b`/`d01c4f9`) and review reply — no new human activity since. Priority 1 not triggered.
- Confirmed the quality-pass self-limiting clause (priority 2) doesn't apply: the sole existing
  report, `reports/quality/2026-08-03-quality-pass.md`, carries zero unresolved `BLOCKING`
  findings (finding #1, `sampleHard`'s unseeded `tf.multinomial`, was fixed and closed in its
  2026-08-04 append; findings #3/#4/#7 remain open but are NOTE/NIT, not `BLOCKING`) — so per
  `loop/GOAL.md`'s "when none remain, move on to priority 3" the quality-pass slot is inactive and
  today's increment falls through to the next actionable item.
- Increment: `docs/explainers/0014-reward-head-spec.md`, new — the docs-only reward-head spec the
  2026-09-14 stand-up's "Tomorrow" line named, itself picked from the two options PR #74's review
  offered. Proposes `RewardHead` (`src/model/rewardHead.ts`), `rewardLoss`
  (`src/model/losses.ts`), and `WorldModel` wiring (seed salt 3, post-transition-state prediction,
  unweighted loss-total addition, `WorldModelStepResult`/`WorldModelNaNError` field additions,
  `freeze.ts`'s `reward` argument) — same "interfaces, invariants, open questions, no `src/`
  changes" treatment as `0012`/`0013`. No `src/` files touched.
- Corrected `0013`'s parenthetical `symlog` suggestion: re-derived `computeReward()`
  (`src/env/gridworld.ts:145-157`) against every experiment's actual config (`grep -rn
  "gridSize\|numLandmarks" experiments/*/run.ts` — all use `DEFAULT_CONFIG`'s `gridSize: 8`,
  `numLandmarks: 2`) and found `reward ∈ [-4.0, 0.0]` — a fixed, single-order-of-magnitude range,
  not the multi-order-of-magnitude spread `symlog` compresses. Proposed a plain linear head + MSE
  instead, citing `reconstructionLoss`'s own doc comment as the precedent for the same reasoning
  already applied to this environment's `Observation` vector.
- Flagged two required updates to `0013` for whenever `RewardHead` actually lands: (1) its
  imagination pseudocode's `rewardHead.predict(...)` push needs a `.dataSync()[0]` conversion to
  match `computeLambdaReturns`'s `number[]` contract, and (2) its WM/AC gradient-separation test
  spec needs a fourth snapshot set (`worldModel.rewardHead.trainableWeights()`) alongside
  `cell`/`decoder`/`continueHead`.
- Verified (after `npm install` — see "Assumptions made"): `npm test` 169/169 (157 pass, 0 fail,
  12 todo) — unchanged, as expected for a docs-only change. `npm run typecheck:ratchet` 44/44
  in-repo count, unchanged, exit 0. `npx markdownlint-cli2 "docs/explainers/**/*.md"` — 0 issues
  across all 14 explainer docs including the new one.

## Learned

- `self_checked, high confidence`: `0013`'s `symlog` suggestion for the (not-yet-built) reward
  head doesn't hold up against this project's actual reward magnitude. It was a reasonable-sounding
  DreamerV3-default carried over without checking `computeReward()`'s actual output range against
  the configs this project runs — worth flagging as a general pattern to watch for in future
  explainers: a paper-default choice stated as fact needs the same "check it against this
  project's actual numbers" treatment as any other claim, not just claims about tfjs API behavior
  (which this project already treats carefully, e.g. `docs/explainers/0011`'s `sigmoidCrossEntropy`
  smoke-check, the 2026-08-04 `tf.multinomial` non-reproducibility finding).
- `self_checked, high confidence`: every experiment script in `experiments/` runs at
  `DEFAULT_CONFIG`'s `gridSize: 8` / `numLandmarks: 2` — none overrides either to a different
  value. This isn't new information specific to this run, but it's the fact this run's reward-range
  derivation depends on, so recording it: if a future arm or environment change widens the grid or
  landmark count, the reward-magnitude conclusion above (and the "no `symlog`" recommendation
  built on it) should be re-checked against that config's own numbers, not assumed to still hold.

## Blockers

- none.

## Decisions needed

- [ ] None blocking.

## Assumptions made (proceeding unless told otherwise)

- Ran `npm install` before verifying (fresh-checkout container, `node_modules` untracked, same gap
  the 2026-09-14 run hit and recorded as "a real, recurring failure mode for this loop, not a
  one-off"). Routine environment setup, not a dependency change: `package.json`/`package-lock.json`
  are untouched, resolved `@tensorflow/tfjs-node` matches the pre-approved `4.22.0` pin exactly.
- This is `0013`'s natural sequel (closing its own named blocking dependency) but has not itself
  been reviewed by the human — `0014` is offered as a proposal, same status `0012`/`0013` had
  before their own reviews landed, not as a pre-approved design. In particular the "no `symlog`"
  recommendation and the reward-head-reads-post-transition-state choice are this run's proposals,
  open to the human's review like any other explainer.
- Kept working on `loop/2026-09-13` (PR #74, still open) rather than opening a new dated branch:
  `0014` cross-references `0013`, which only exists on this branch (PR #74 was never merged, "Not
  merging" in the 2026-09-13 review), and the immediately preceding run already established this
  precedent for the same reason. This adds a new file rather than amending `0013` itself, so
  unlike the 2026-09-14 commit this one doesn't touch content the open review thread was anchored
  to.

## Tomorrow

- If the human's review of `0013`/`0014` raises replies, priority 1 processes them next. Otherwise,
  the natural next increment is either implementing `RewardHead` for real (promoting `0014` from
  spec to code, `src/model/rewardHead.ts` + `src/model/losses.ts`'s `rewardLoss` + the
  `WorldModel`/`freeze.ts` wiring, all inside priority 3's "RSSM completion" scope and this run's
  own allowed paths) or continuing priority 6 (further G2 module spec work) if the human prefers a
  second review pass on `0013`+`0014` together before any implementation lands.

## Manifest

- level: L2-P2-slice | increment: priority 3 (explainer-before-implement for the reward head,
  RSSM-completion-adjacent — sequenced from the 2026-09-14 stand-up's "Tomorrow" line) | files
  touched: 2 (`docs/explainers/0014-reward-head-spec.md`, `reports/standup/2026-09-15.md`) | runs:
  none
