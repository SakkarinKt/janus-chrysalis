# Stand-up — 2026-09-01 (run 1)

## Done
- Processed PR #61's human reply (posted 2026-09-01T00:32:17Z, on the merge
  comment) under priority 1. It did three things:
  1. Answered both reviewer notes on the binomial-statistics extraction and
     merged — no follow-up required; the two nits it raised (`2 ** n`
     overflow near `n≈1030`, `binomialUpperTail(k > n)` returning 0 instead
     of throwing) are explicitly "NOT blocking" and confirmed unreachable
     from any current call site.
  2. Instructed: "raise the continue/termination-head question under
     'Decisions needed' with options, not a decision" — done below, this
     run's increment.
  3. Confirmed the three telemetry follow-ups (from PR #60's review —
     `dirty`/git-clean field, `writeTelemetry(dir, [])` one-blank-line
     output, missing-trailing-separator path bug) stay exactly where #60
     put them: folded into the next `telemetry.ts` touch, no increment of
     their own. Nothing to do here; carrying them forward unchanged.
- This run's artifact is this report plus the "Decisions needed" item below
  — no `src/`/`test/` changes. Verified the ask is genuinely a
  write-up-only task, not a coding one, before treating it as complete:
  re-read `src/env/gridworld.ts`, `PLAN.html`, and `docs/proposals/0001` to
  ground the options in the actual code rather than restating the review
  comment.

## Learned
- `PLAN.html:110` lists "Continue/termination head from day one" as one of
  Phase 2's ported invariants, in the same clause as KL free-bits, WM/AC
  gradient separation, λ-returns, and NaN-halt. No continue-head code exists
  in `src/model/` yet (`decoder.ts`, `losses.ts`, `rssm.ts`,
  `worldModel.ts` — grepped for `continue`/`terminat`, no matches). The
  other four invariants in that clause are well-posed regardless of the
  environment (they're properties of the model's math), but the continue
  head's target is `done` from the environment — and Arm-A's gridworld
  (`src/env/gridworld.ts:57,65-66`) only ever sets `done = currentStep >=
  horizon`, a deterministic function of the step counter with no stochastic
  or state-dependent component. "Day one" didn't anticipate that gap;
  self_checked, confidence: high (grounded in the grep + the read env code,
  not inference).
- Every proposal-`0001` run recorded so far (2026-08-13 through 2026-08-27,
  matched-seed and power-analysis runs alike) used this same
  horizon-only `done` at a fixed `HORIZON=75`. Any change to episode
  semantics is therefore a comparability break against the whole existing
  results table, not just a code change — which is why PR #61's reply
  named it the human's call rather than something to default on.
  self_checked, confidence: high.

## Blockers
- none. This decision blocks only the continue/termination-head sub-piece
  of priority 3 (RSSM completion) — the KL-balanced loss, observation
  reconstruction, and rollout wiring into `freeze.ts` don't depend on it
  and can proceed in a future run regardless of which option is chosen.

## Decisions needed
- [ ] Continue/termination head (`PLAN.html:110`): Arm-A's gridworld has no
  real early-termination condition — `done` is horizon-only
  (`src/env/gridworld.ts:57,65-66`). Three ways to close this: **(A)**
  implement the head against the existing deterministic horizon boundary
  (predict `step < horizon`) — cheapest, ships "day one" literally, zero
  comparability impact, but the target is a known function of the step
  index the model already sees, so it's scaffolding (shape/gradient-wiring
  test) rather than a genuine prediction task; **(B)** add a real
  stochastic/state-dependent early-termination condition to the env
  (e.g. a hazard cell, early task-success stop) so the head predicts
  something meaningful — breaks comparability with every proposal-`0001`
  run recorded so far, needs baseline re-runs or an explicit arm fork;
  **(C)** defer the continue/termination head past this vertical slice,
  record it as documented plan-drift against `PLAN.html:110`
  (recorded-but-undone), revisit if/when a later phase needs a real
  termination signal. Which one?

## Assumptions made (proceeding unless told otherwise)
- Treated PR #61's reply as this run's full priority-1 increment (raise the
  question, confirm the deferred items, ship the report) rather than also
  starting priority 3 (RSSM completion) in the same run — GOAL.md's "never
  ... work on more than one increment per run" reads this as one increment,
  and the reply didn't ask for code this run.
- Kept the continue/termination-head write-up inside this stand-up's
  "Decisions needed" section rather than a new `docs/explainers/` note,
  since the ask ("raise ... under Decisions needed") named that section
  specifically and the three options above are answerable in one line.

## Tomorrow
- Pending the human's answer above: (A) or (C) can start immediately as
  priority 3 (RSSM completion) or its own small increment; (B) would need
  scoping as its own increment (env change + baseline re-run plan) before
  any code lands.
- If no reply lands on this PR before the next scheduled run and no other
  PR carries an unanswered reply, fall through to priority 2 (the existing
  `reports/quality/2026-08-03-quality-pass.md` — check for unresolved
  `BLOCKING` findings and fix the smallest one) per GOAL.md's priority
  list, since priority 3's continue-head sub-piece is gated on this
  decision and the rest of priority 3 (losses + rollout wiring) is also a
  valid fallback if the quality pass has no open `BLOCKING` items.

## Manifest
- level: L2-P2-slice | increment: 1 (processing PR #61's human reply — raised the continue/termination-head decision, confirmed deferred telemetry follow-ups) | files touched: 1 (this report) | runs: none
