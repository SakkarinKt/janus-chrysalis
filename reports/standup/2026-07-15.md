# Stand-up — 2026-07-15 (run 12)

## Done

- Checked PRs #7–#14 first per `loop/GOAL.md` priority 1. PR #14 (merged) carries an unanswered
  human review: **approve**, with a concrete next-step instruction — "go ahead and start the RSSM
  world-model cell... Split the cell as you proposed (struct/forward-pass, then STE +
  gradient-check)" — plus one deferred cleanup ("for the cell PR, not this one": update ADR-0002 §7's
  inline "custom cell + `tf.customGrad`" phrase to `tf.stopGradient`, matching the robustness note's
  §4). Processing this reply is this run's increment.
- Before any cell code can be written or tested, the world model needs an actual TF.js backend —
  `package.json` has never had a dependency on `@tensorflow/tfjs`/`tfjs-node`. `loop/GOAL.md`'s
  pre-approved dependency carve-out requires this to land in **its own PR/commit** touching only
  `package.json` + lockfile + a trivial smoke import, not bundled into a larger scaffold PR. That
  makes it the correct bounded increment ahead of the cell itself, not a substitute for it:
  - `package.json` / `package-lock.json`: added `@tensorflow/tfjs-node`, pinned exactly to `4.22.0`
    (last stable release per ADR-0002 §1, not the `4.23.0-rc.*` pre-release) via
    `npm install @tensorflow/tfjs-node@4.22.0 --save-exact`.
  - `test/smoke/tfjs-node.test.ts` (new): loads the native binding, runs one tensor op, asserts the
    result and that the backend is `"tensorflow"` (CPU, matching ADR-0002 §7's backend decision) —
    exactly the "surface Apple-Silicon native-build failures early" smoke test `loop/GOAL.md`
    describes.
  - `notes/adr-0002-js-ml-stack.md` §7: dated addendum recording the dependency landing, the
    successful smoke test on this session's Linux x64 sandbox, and that Apple Silicon install
    friction (§3) remains unverified since this environment isn't `darwin-arm64`.
  - `docs/proposals/0001-direct-nonstationarity-measurement.md`: one-line dated status update noting
    PR #14's approval and that the dependency prerequisite is now landed, cell still unwritten.
  - Did **not** touch ADR-0002 §7's `tf.customGrad` → `tf.stopGradient` cleanup — the reviewer named
    that for the cell PR specifically, not this one.

`npx markdownlint-cli2 "**/*.md"`: 0 errors (28 files). `npm test`: 28/28 passing (27 prior + 1 new
smoke test). No RSSM cell code, no other dependencies, no training runs this run.

## Learned

- **[high, self_checked]** `@tensorflow/tfjs-node@4.22.0` installs and loads its native binding
  cleanly on this session's Linux x64 sandbox (Node v22.22.2) — `npm install` completed without a
  from-source rebuild, and the smoke test's tensor op returns the correct result on the CPU
  (`"tensorflow"`) backend. This is a new, direct data point (not literature-derived) confirming
  ADR-0002 §7's "CPU/tfjs-node, not WebGPU" backend decision is buildable in at least this
  environment. It does **not** confirm the Apple Silicon story from ADR-0002 §3 (that note's install
  friction reports were about `darwin-arm64`, which this sandbox isn't) — that risk stays open until
  tested on the human's actual machine.
- **[medium, self_checked]** `npm audit` flags 3 high-severity advisories, all transitive via
  `@mapbox/node-pre-gyp`'s bundled `tar` version (install-time-only, used to fetch/extract the
  prebuilt native binary — not part of the runtime dependency graph the RSSM cell's ops would
  execute). `npm audit fix --force`'s suggested remediation would downgrade
  `@tensorflow/tfjs-node` to `0.1.11`, an ~8-major-version regression that's obviously the wrong
  fix. Consistent with ADR-0002 §1's "low-maintenance-cadence, large backlog" finding about the
  broader TF.js dependency tree — noted, not acted on; flagging in case the human wants a closer
  look at whether the install-time-only distinction is acceptable long-term.
- **[low, self_checked]** The milestone's actual next step — the RSSM cell's struct/forward-pass
  sub-increment the reviewer named — is now unblocked with no further prerequisite: gate cleared
  (PR #14), human approval given (PR #14 review), dependency landed (this run). Nothing else stands
  between here and writing it.

## Blockers

- none

## Decisions needed

- none this run — PR #14's review already answered the open questions from prior runs; this run
  only processed that reply and landed its hard prerequisite.

## Assumptions made (proceeding unless told otherwise)

- Read "start the RSSM world-model cell, split struct/forward-pass first" as authorizing the
  prerequisite dependency work needed to make that possible, not as requiring the cell's own code to
  land in this same run — `loop/GOAL.md`'s dependency boundary explicitly forbids bundling the two
  into one PR, and one bounded increment per run already caps today's scope. Treated the dependency
  landing as this run's full increment rather than a partial one.
- Used `npm install --save-exact` (pins `"@tensorflow/tfjs-node": "4.22.0"` with no `^`/`~` range) —
  matches `loop/GOAL.md`'s "pinned to 4.22.0" wording literally; flagging in case a caret range was
  intended instead.
- Did not run `npm audit fix` in any form given the downgrade it proposes is clearly wrong at this
  scale; left the advisories documented instead of suppressed.

## Tomorrow

- The RSSM world-model cell, struct/forward-pass sub-increment (per PR #14 review): the cell's
  state/parameter shape and a `tf.layers.gru`-based deterministic recurrent step, no STE or
  gradient-check yet — those are explicitly the following sub-increment. Per `CONTRIBUTING.md`'s
  explain-before-implement convention, this likely needs a `docs/explainers/0003-...md` note ahead
  of (or alongside) the implementation itself, since a world-model cell is squarely a "core
  algorithm."
- Follow-on sub-increment after that: the categorical-latent straight-through path
  (`tf.stopGradient`) plus the finite-difference gradient-check test the robustness note's §5
  recommends land with the cell, and the ADR-0002 §7 `tf.customGrad`→`tf.stopGradient` wording
  cleanup PR #14's review named for that PR specifically.

## Manifest

- level: L2-Arm-A | increment: 2 (Arm-A milestone, world-model-cell dependency prerequisite) | files touched: 6 | papers added: 0
