# Stand-up — 2026-07-07 (run 4)

## Done

- Checked PR #4 (`loop/2026-07-06`, merged) for unanswered items: no human comments or reviews
  exist on it (confirmed via GitHub API — 0 comments, 0 reviews), even though its own body shows
  both "Decisions needed" checkboxes as `[x]` (an authoring artifact from that run, not an actual
  human reply — no reply exists to point to). Since the lit map had already reached its 10-paper
  target and no new research-direction ambiguity was raised, I resolved both items myself by
  proceeding per `loop/GOAL.md` priority 3 and recording the reasoning in
  `docs/proposals/gap-analysis.md` §4, rather than blocking this run on a reply that never arrived.
  Flagging this explicitly below as a "Decisions needed" item so a human can confirm or correct
  the call.
- Completed today's increment: **priority 3 — gap-analysis and first research-proposal draft**.
  - `docs/proposals/gap-analysis.md` (new): synthesizes `notes/lit-map.md`'s 10 papers into a
    3-dimensional design space (sharing topology / policy-execution scope / non-stationarity
    mechanism — the map's original single axis undersold the space once all 10 notes were read
    together), names the core empirical gap (no paper directly measures non-stationarity itself),
    and the secondary gap (no controlled single-codebase topology sweep exists).
  - `docs/proposals/0001-direct-nonstationarity-measurement.md` (new, using
    `docs/proposals/TEMPLATE.md`): first proposal draft. Hypothesis: a freeze-intervention can
    isolate world-model prediction error attributable specifically to a co-learning partner's
    policy drift, and this drift-attributable error differs systematically across 4 sharing
    topologies (independent / peer-comms / centralized-aggregation / shared-joint), holding
    environment, backbone, and compute fixed. Includes a laptop-scale custom TS grid-world design
    (no PettingZoo/JAX dependency), 3 ablations, success/kill criteria, and a cost estimate
    (~40 overnight runs).
  - `notes/lit-map.md`: one-line status update pointing at the new proposal work; no paper
    additions this run (that's not today's increment).
- Ran `npx markdownlint-cli2` against all 3 changed/added files — 0 errors.

## Learned

- **[medium, self_checked]** Reading all 10 lit-map notes together for the gap analysis surfaced
  that "sharing topology" (the map's original organizing axis) conflates two independent design
  choices: where the *world model* sits (topology) and where the *policy execution* sits (scope).
  `mmsa-2026` and `matwm-2025` both pair a shared/joint world model with fully decentralized
  execution — CTDE for the model does not imply CTDE for the policy. Recorded as a named,
  separate dimension in `docs/proposals/gap-analysis.md` §1 rather than folded into the existing
  axis.
- **[medium, self_checked]** The 10-paper map, read as a set rather than paper-by-paper, supports a
  concrete, falsifiable, laptop-scale first proposal without needing an 11th paper: the gap isn't
  "we lack an architecture," it's "no one measures the thing every architecture claims to fix."
  That reframing (measurement-first rather than architecture-first) is the proposal's core bet.
- **[low, self_checked]** Confirmed markdownlint's current config (from PR #3's OSS-hardening pass)
  is permissive enough that a long-form proposal doc with prose paragraphs passes cleanly with no
  special-casing needed.

## Blockers

- none

## Decisions needed

- [ ] PR #4's two "Decisions needed" checkboxes were marked `[x]` in that run's own report with no
      corresponding human comment or review on GitHub. I've treated this as an authoring mistake
      (the previous run checking its own open questions rather than a human answering them) and
      resolved both myself this run (move to gap-analysis now; hold Drama and "Communicating
      Plans, Not Percepts" to qualitative-design-input status only, per `gap-analysis.md` §4). Was
      that the right call, or should future stand-ups only ever check items a human has actually
      replied to, leaving self-resolved items unchecked with a note instead?
- [ ] `docs/proposals/0001-direct-nonstationarity-measurement.md`'s freeze-intervention design and
      4-arm topology sweep — sound enough to move toward implementation (which would require an L2
      promotion, the human's call per `loop/GOAL.md`), or does it need another draft pass first?
- [ ] The gap analysis flags a second possible proposal (policy-execution-scope as an independent
      variable, `gap-analysis.md` §5) but wasn't drafted this run to keep to one increment — should
      that be `0002`'s focus next, or should effort go toward refining `0001` instead?

## Tomorrow

- If `0001` looks sound: begin refining it toward implementation-readiness (environment spec
  details, exact backbone pending ADR-0002) while staying at L1 (draft-only, no code) until the
  human promotes to L2.
- Otherwise: revise `0001` per feedback, or draft `0002` (policy-execution-scope proposal) instead.
- Either way: JS ML stack notes toward ADR-0002 (priority 4 — TF.js maintenance, WebGPU maturity,
  tfjs-node on Apple Silicon, custom-autograd feasibility) remain untouched and are a fallback
  increment if proposal work stalls on human input.

## Manifest

- level: L1 | increment: 3 (gap-analysis + first research-proposal draft) | files touched: 3 | papers added: 0
