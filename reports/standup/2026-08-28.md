# Stand-up — 2026-08-28 (run 1)

## Done

- Processed PR #56's review comment (@SakkarinKt) per `loop/GOAL.md` priority 1 — it's the most
  recent reply on a `loop/*` PR and, although the PR was already merged, the review left a latent
  bug and a process note that needed addressing before starting new research work.
- Fixed the latent bug the review flagged in `experiments/2026-08-27-high-visibility-preregistered/run.ts`'s
  `binomialTwoSided(k, n)`: the `k === 0 ? 1` special case for `P(X<=k)` was wrong. The general
  formula `binomialUpperTail(n - k, n)` already gives the right value at `k=0` too — it sums only
  the `i = n` term of the loop, i.e. `C(n,n)/2^n = 1/2^n`, which *is* `P(X<=0)`. The special case
  instead forced `lower = 1`, so `binomialTwoSided(0, n)` always returned `p = 1` regardless of
  `n`, when the correct value is `2/2^n` (e.g. `0.25` at `n=3`, matching the review's own worked
  example). Removed the special case (one line).
- Verified the fix doesn't change any number this investigation has reported: the only live call
  site is `binomialTwoSided(combinedNeg, combinedN)` with `combinedNeg=8, combinedN=12` (k≠0), and
  a standalone check of both the old and fixed function against that input and several others
  (`k=0` at `n=1,3,9,12`; `k=n`; the actual `k=8,n=12` and `k=1,n=9` cases from this
  investigation) confirms: every `k≠0` case is byte-identical before/after; only `k=0` changes,
  from `1.0` to `1/2^(n-1)`. The bug was exactly as "unreachable as the script stands" as the
  review said — this run's `combinedP=0.387695` is unaffected.
- `npm install` (`node_modules` not present in this environment): clean install against the
  committed lockfile, no dependency changes. `npm test`: 124/124. `npm run typecheck:ratchet`:
  unchanged at 44/44, exit 0.

## Learned

- The review's math check was itself correct and specific enough to reproduce exactly: rebuilding
  `binomialUpperTail`/`binomialTwoSided` standalone and running the `n=3` example gave the same
  `0.25` the review predicted, before touching the real file.
- The review's larger point — that PR #56 (run 3) should not have happened at all, because PR
  #55's review said "do not act on this comment now ... pick it up as the priority-1 item on the
  next *scheduled* run" and run 3 acted on it 40 minutes later anyway — is about this loop's own
  process, not the science. Re-reading the three same-day PRs (#54, #55, #56) in sequence: #55's
  hold was explicit and unambiguous, and it was overridden without being acknowledged in #56's PR
  body. This run is the actual "next scheduled run" the hold was written for, and there is nothing
  left to act on from it (PR #55's "Decisions needed" item was already answered and executed in
  #56, and the review confirmed #56's science is sound) — so no repeat action is taken here, just
  the bug fix and the acknowledgment below.

## Blockers

- none

## Decisions needed

- [ ] PR #56's review asked, verbatim, for "a `loop/GOAL.md` rule that an explicit 'do not act on
  this resume' in a review overrides the priority-1 human-reply trigger" before priority 3 work
  resumes. I can't edit `loop/GOAL.md` myself. Do you want that rule added (and if so, should it
  cover *any* explicit hold instruction in a review, not just ones phrased exactly as "do not act
  on this resume"), or was the process violation itself the only thing you wanted on record?

## Assumptions made (proceeding unless told otherwise)

- Treated processing PR #56's review as this run's whole increment (priority 1), rather than
  also starting priority 3 work in the same run — the review's flagged bug and unacknowledged
  process violation read as unfinished business from the last cycle, and `loop/GOAL.md` caps this
  run at one increment regardless.
- Confirmed `reports/quality/2026-08-03-quality-pass.md` carries no more open `BLOCKING` findings
  (finding #1 closed 2026-08-04 per that report's own resolution note; remaining #3/#4/#7 are
  `NOTE`/`NIT`), so priority 2's slot is clear and priority 3 (RSSM completion) is next up, exactly
  as PR #56 already concluded — not started this run, just confirmed and queued.

## Tomorrow

- RSSM completion (`loop/GOAL.md` priority 3): the world-model losses (KL balancing with a
  free-bits floor, observation reconstruction) and wiring `RSSMCell` into
  `src/experiment/freeze.ts`'s rollout. Explainer-before-implement applies.

## Manifest

- level: L2-P2-slice | increment: 1 | files touched: 2 | runs: none
