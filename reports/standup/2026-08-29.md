# Stand-up — 2026-08-29 (run 1)

## Done
- Priority 5 (vertical-slice hardening) sub-item: the "NaN → graceful halt" invariant, one of
  `loop/GOAL.md`'s four named priority-5 invariant tests. Before this run, nothing in `src/`,
  `test/`, or `experiments/` checked for `NaN`/non-finite values at all (`grep -rn -i
  "halt\|isNaN" src/ test/ experiments/` returned nothing) — a `WorldModel.step()` whose loss went
  `NaN` would silently commit that state and keep training/evaluating on it every step after,
  invisibly, with no error until (or unless) something far downstream happened to notice.
  - `src/model/worldModel.ts`: new `WorldModelNaNError` (loss/reconstructionLoss/klLoss fields).
    `WorldModel.step()` now checks `Number.isFinite` on all three after `forward()` completes
    (not the existing catch block's path — this is a value that computed successfully and is
    non-finite) and throws instead of committing state, disposing exactly what that step
    allocated and leaving `this.state` untouched — same contract the existing throw-path catch
    block already gives a genuine exception from `forward()`.
  - `test/model/worldModel.test.ts`: two new tests (split by `train`, since the two branches
    genuinely differ — see below), verifying the throw, state/tensor-lifecycle correctness, and
    (train=false only) full recovery afterward.
  - `docs/explainers/0009-worldmodel-nan-halt.md`, new: full design writeup, explainer-before-
    implement per `loop/GOAL.md`.
  - `npm test`: 126/126 (was 124/124). `npm run typecheck:ratchet`: unchanged at 44/44, exit 0.

## Learned
- **PR #57's review named an already-completed increment.** The review (this morning,
  2026-08-29T00:57 UTC) said "Next: priority 3, RSSM completion — KL balancing with a free-bits
  floor, observation reconstruction, and wiring RSSMCell into `src/experiment/freeze.ts`'s
  rollout." Checked against `git log` before starting: all three landed weeks ago as explicit
  sub-increments — PR #38 (KL-balanced loss), PR #39 (rollout wiring), PR #40 (reconstruction
  loss) — and priority 4 (Arm-A metric plumbing + instrument validation) is also done (PR #42,
  PR #45). `loop/GOAL.md`'s "Today's increment" priority list hasn't been updated to reflect this;
  the file predates most of that work (last dated change: the 2026-07-29 mid-phase amendment,
  which touched a different section). Did not redo the named work — see "Decisions needed" below
  — and instead continued down the same priority-ordered list to the first item with real
  remaining work (priority 5's NaN-halt invariant; full reasoning in `docs/explainers/0009`'s
  first section, including why the other three priority-5 invariants either already exist,
  aren't yet applicable, or are correctly out of scope for this entry).
- **`loop/GOAL.md`'s own quality-pass lens language ("NaN-halt actually fires," priority 2's bug-
  hunt list) forward-referenced a mechanism that didn't exist yet.** It does now; a future
  quality-pass run's numerical-stability lens has something real to check.
- **A `train: true` NaN-halt throw cannot undo the optimizer step that already ran** —
  `tf.variableGrads`/`applyGradients` execute before this function ever gets to read the loss back
  and check it. Confirmed directly, not assumed: the new train=true test asserts at least one cell
  weight is literally `NaN` immediately after the caught throw. The halt still stops the
  *recurrent state* from carrying that corruption into further steps and stops the failure from
  being silent, but a `train: true` NaN event leaves the model needing re-init (or, once one
  exists, a checkpoint restore) — documented as a limitation in `docs/explainers/0009`, not
  silently glossed over. `train: false` (the frozen-agent evaluation path proposal `0001` depends
  on) has no such limitation — verified full recovery after a caught throw on that path.
- TS parameter-property shorthand (`constructor(readonly x: number)`) doesn't survive this
  project's type-stripping-only Node execution — hit `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` on the
  first draft of `WorldModelNaNError`, fixed by declaring fields explicitly and assigning in the
  constructor body (same constraint `src/env/types.ts`'s `Action` const-object comment already
  documents for `enum`, now hit directly rather than just cited).

## Blockers
- none

## Decisions needed
- [ ] PR #57's review pointed the next run at priority 3 (RSSM completion), but `git log` shows
      all of it landed already (PR #38/#39/#40), as did priority 4 (PR #42/#45). Is
      `loop/GOAL.md`'s "Today's increment" list meant to be re-read fresh each run against actual
      repo state (in which case this run's choice to continue down the list to priority 5 was
      correct and no `loop/GOAL.md` change is needed), or should the file be updated to strike
      the now-done items so this ambiguity doesn't recur? Left `loop/GOAL.md` untouched either way
      (can't edit it) and did not redo the named work.

## Assumptions made (proceeding unless told otherwise)
- Treated "continue down `loop/GOAL.md`'s priority-ordered list to the first item with real
  remaining work" as a sensible default when the human's most recent direction named an
  already-completed increment, rather than as a decision requiring a same-day answer — recorded
  above under "Decisions needed" for the human to confirm or correct, not blocked on it.
- Scoped the NaN-halt check to `!Number.isFinite` (catches `±Infinity` too), not literally
  `Number.isNaN` alone — `Infinity` is the same failure class and would otherwise silently
  propagate the same way. Full reasoning in `docs/explainers/0009`.
- Left `WorldModelNaNError` uncaught by every existing caller (`src/experiment/freeze.ts`'s
  `runEpisode`, the `experiments/*/run.ts` scripts) — today, a non-finite world-model loss stops
  the whole process rather than just the offending episode. Strictly better than the silent-
  corruption status quo this entry replaces, and "halt" doesn't specify "keep the rest of the
  process running" — but graceful-at-the-episode-level recovery is a real follow-up, not done
  here to keep this entry to the one invariant `loop/GOAL.md` names.

## Tomorrow
- Priority 5's remaining invariant: the continue/termination head (`PLAN.html` Phase 2: "Continue/
  termination head from day one"; `docs/explainers/0006` already flagged this as deferred to
  priority 5). Needs its own explainer-before-implement pass — a new network head, a loss term,
  and a design decision about what "termination" means in an environment whose `done` is purely
  `horizon`-based today (`src/env/gridworld.ts`, confirmed no early-termination condition exists).
  Alternatively, if the human's PR #57 review intended something more specific by "priority 3"
  than what's already landed, that supersedes — see "Decisions needed" above.

## Manifest
- level: L2-P2-slice | increment: 5 (vertical-slice hardening — NaN → graceful halt invariant) |
  files touched: 3 | runs: none
