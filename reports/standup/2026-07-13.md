# Stand-up — 2026-07-13 (run 7)

## Done

- Checked PRs #7–#12 per `loop/GOAL.md` priority 1. PR #12 (merged) carries an unanswered human
  review comment with two concrete follow-up items flagged "not blocking this merge" but to
  "carry into the world-model-cell increment." Processing that reply was this run's increment.
  - **Applied**: `EpisodeStepRecord.observations` was misleadingly holding the *post*-step
    observation, forcing a future metrics module to pair each record with its predecessor to
    recover the pre-step obs — and the reset observation (predecessor of the first record) was
    never captured at all. `src/experiment/freeze.ts` now records both: `observations` (pre-step,
    what each policy acted on — the reset observation for step 1) and `nextObservations`
    (post-step, renamed from the old field), matching `Transition`'s existing
    `observation`/`nextObservation` naming.
  - **Tracked, not applied**: the reviewer's structural-gating ask ("one gated update path" so a
    future world-model update can't bypass the freeze gate the way a second, ungated hook would).
    Today `runEpisode` only gates `Policy.update()` because that's the only learning hook that
    exists — there's no world-model update path yet to miss, and designing one now means guessing
    the still-gated world-model cell's shape before the RSSM-vs-SSM/Mamba robustness note has
    even started. Recorded explicitly in the explainer as a design contract for whoever builds
    that cell, rather than speculatively implemented today.
  - `docs/explainers/0002-freeze-mechanism.md`: new "PR #12 review follow-ups" section documenting
    both items and their disposition; test-coverage summary updated.
  - `test/experiment/freeze.test.ts`: one new test chaining `records[i].observations` against
    `records[i-1].nextObservations` and checking the first record's `observations` equals the
    reset observation.
- `node --test`: 27/27 passing (was 26/26 before this run's added test).
- `npx markdownlint-cli2` on the changed doc: 0 errors.

No new code paths, dependencies, or training runs. No metric plumbing or RSSM-vs-SSM/Mamba
robustness note this run — the reviewer's own comment on PR #12 recommended the robustness note
next, which is priority 3 and still not started; today's increment was fully consumed by
processing the PR #12 reply per priority 1.

## Learned

- **[medium, self_checked]** The "one gated update path" concern raised in PR #12's review can't
  be structurally closed without knowing the world-model cell's shape — attempting to do so now
  would mean designing against a component that's still gated behind an unwritten robustness
  note, i.e. the same kind of premature-abstraction risk the project explicitly wants avoided.
  Documenting it as a contract for that future increment is the right amount of action *now*;
  actually enforcing it belongs to the cell's own design pass.
- **[low, self_checked]** Storing both `observations` and `nextObservations` per step (rather than
  renaming to just `nextObservations` and leaving pairing-with-predecessor to the metrics module)
  also fixes the reset-observation gap for free — the first record's `observations` is exactly the
  `env.reset()` output already held in a local variable, so no extra plumbing was needed.

## Blockers

- none

## Decisions needed

- none this run.

## Assumptions made (proceeding unless told otherwise)

- Treated "process PR #12's human reply" (priority 1) as this run's one increment, rather than
  starting priority 3 (RSSM-vs-SSM/Mamba robustness note) even though the reviewer suggested it —
  the reviewer's own comment on PR #12 was itself an unprocessed human reply with a concrete
  requested change (the field rename), so closing that out took priority per `loop/GOAL.md`'s
  ordering. The robustness note is queued as tomorrow's increment.
- Chose "store both obs" over "just rename to `nextObservations`" (the reviewer offered either) —
  named in "Learned" above; flagging in case the reviewer would have preferred the smaller diff.
- Left the structural-gating item as a tracked doc note rather than a code change — see "Done"
  above; flagging in case the reviewer wants a stronger placeholder (e.g. a runner-level assertion
  stub) even before the world-model cell exists.

## Tomorrow

- RSSM-vs-SSM/Mamba implementation-robustness note (`loop/GOAL.md` priority 3) — unblocks the
  world-model cell, and is what PR #12's reviewer recommended next. Metric plumbing (priority 2's
  next sub-increment) remains available as an alternative once there's something to measure
  prediction error against, but the robustness note is the more direct path to unblocking the
  cell both the metric and the structural-gating follow-up ultimately depend on.

## Manifest

- level: L2-Arm-A | increment: 1 (process PR #12 human reply: rename/dual-track
  `EpisodeStepRecord` observation fields, document deferred structural-gating follow-up) | files
  touched: 3 | papers added: 0
