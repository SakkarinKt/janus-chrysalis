# Stand-up — 2026-08-27 (run N)

## Done

- Processed PR #53's review (@SakkarinKt, posted as an issue comment after merge) — today's
  increment per `loop/GOAL.md` priority 1 (unanswered human reply on a `loop/*` PR). The review
  verified 2026-08-26's data as correct but caught two conceptual errors in that PR's "What this
  does and doesn't settle" paragraph, and directed the next step.
- `docs/proposals/0001-direct-nonstationarity-measurement.md`: corrected the paragraph (dated
  2026-08-26/27 inline, this doc's usual correction convention). (1) `driftAttributableError` is
  `intervention - control`, and 2026-08-13's "gate (b)" convention reads **positive** `diffMean` as
  the predicted-direction result (frozen agent's error rising above the both-frozen control). The
  n=9 radius-6 pool's 8-negative/1-positive split is therefore evidence *against* the predicted
  drift direction — the paragraph had originally called it "the strongest evidence to date for some
  real effect," backwards. (2) All nine pooled rows are at the same radius (6) — zero radius
  contrast — so the result says nothing about partner-`viewRadius` as a variable, which the original
  framing implied without the data to support it.
- Ran `experiments/2026-08-26-radius4-matched-seeds/run.ts`: the review's directed next step — a
  matched seed set at radius 4 (the same nine seeds already run at radius 6: 1001-1009), building
  the actual radius contrast the corrected paragraph needed. Radius-6 `diffMean` values read from
  `artifacts/2026-08-26-radius6-more-seeds/pooled-radius6.summary.csv`, not rerun. Full write-up:
  the proposal doc's "2026-08-27 update"; manifests + summary CSV committed under
  `artifacts/2026-08-26-radius4-matched-seeds/` (telemetry gitignored per the existing
  `artifacts/**` rule; directory kept the 2026-08-26 date stamp since the run was conceived and
  executed that day, crossing into 2026-08-27 only by the few seconds it took to finish — the
  stand-up/branch/PR use today's actual date instead).

`npm test`: 124/124 (unchanged — no `src/` changes this run). `npm run typecheck:ratchet`:
unchanged at 44, exit 0. The new experiment file spot-checked separately (`tsconfig.json` doesn't
cover `experiments/`) with matching compiler flags — same 32 pre-existing/library diagnostics as
every prior equivalent file, none in the new file itself.

## Learned

- **Radius 4's sign pattern looks nothing like radius 6's** (`self_checked, high confidence`):
  radius 4 mean(diffMean) = +0.0371 (positive), sign split 5 negative / 3 positive / 1 exactly zero
  (seed 1008) — versus radius 6's mean -0.0800, split 8 negative / 1 positive. Only 6/9 seeds share
  the same sign at both radii; under an independent-coin-flip null, 6-or-more-of-9 matches has
  `p≈0.254` — not distinguishable from chance.
- **This argues against a partner-`viewRadius`-linked mechanism explaining radius 6's earlier
  result** (`self_checked, medium confidence`): if radius 6's negative skew reflected something
  these seeds' geometries do consistently at that radius (or under partner-`viewRadius` generally),
  the same seeds should skew the same way more often than chance at radius 4. They don't. The
  simpler reading is that radius 6's `p≈0.039` was itself a chance pattern surfaced by one look
  among several this investigation has made on the same dataset — consistent with, not proof of,
  a pure-sampling-noise account.
- **Track record on this measurement axis**: four independent looks (the 2026-08-23/24 3-radius
  sweeps, the n=9 radius-6 pool, this radius-4 match) and none has survived its own follow-up
  check. Worth weighing explicitly when deciding whether to keep investing increments here.

## Blockers

- none

## Decisions needed

- [ ] Given four looks at partner-`viewRadius`'s effect on drift-attributable-error sign/magnitude
      with none surviving follow-up, should the next increment (a) continue on this same
      measurement design — more radii and/or more seeds, now informed by this track record, or (b)
      move to a different `loop/GOAL.md` priority-list item (priority 3's RSSM completion, priority
      5's vertical-slice hardening, priority 6's G2 module specs) — none of which depend on this
      question being resolved first? Sharpened from 2026-08-24's/25's/26's stand-ups; this run's
      result weakens the case for a real effect rather than strengthening it, reversing the prior
      trend.

## Assumptions made (proceeding unless told otherwise)

- Kept `experiments/2026-08-26-radius4-matched-seeds/`'s and
  `artifacts/2026-08-26-radius4-matched-seeds/`'s 2026-08-26 date stamp even though the run
  actually finished a few seconds into 2026-08-27 UTC — the increment was conceived, coded, and
  almost entirely executed on 2026-08-26 (processing that day's PR #53 review), matching this
  project's existing convention of naming experiment directories for the day the underlying
  increment was worked, not the exact wall-clock second output was written. The branch, PR title,
  and this report use 2026-08-27, the actual date `date +%Y-%m-%d` reports, per `loop/GOAL.md`'s
  literal instruction for those three.
- Interpreted "matched seed set at radius 4" as the *same* nine seeds already run at radius 6
  (1001-1009), not a fresh independent batch — the review's own reasoning ("builds the radius
  contrast the sentence currently assumes") only works as a paired, not independent, comparison.

## Tomorrow

- Pending the "Decisions needed" answer above: either further work on the partner-`viewRadius`
  measurement axis (more radii/seeds) or a pivot to `loop/GOAL.md` priority 3 (RSSM completion —
  world-model losses, `RSSMCell` rollout wiring), priority 5 (vertical-slice hardening), or priority
  6 (G2 module specs).

## Manifest

- level: L2-P2-slice | increment: 1 (processing PR #53 review) | files touched: 3
  (`docs/proposals/0001-direct-nonstationarity-measurement.md`,
  `experiments/2026-08-26-radius4-matched-seeds/run.ts`, `reports/standup/2026-08-27.md`) | runs:
  `artifacts/2026-08-26-radius4-matched-seeds/` (18 manifests: 9 seeds × 2 conditions)
