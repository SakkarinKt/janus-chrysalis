# Stand-up — 2026-09-04 (run 1)

## Done
- Processed PR #64's human reply (posted 2026-09-03T13:55:11Z, the merge comment) under priority 1.
  It merged #64, answered the reviewer note on `FREEZE_STEP`/`HORIZON`, and raised two non-blocking
  doc follow-ups plus one carry-forward item:
  1. **`metrics.ts:16` vs. the `0007` addendum contradicted each other** on the continue-head
     target's post-freeze behavior. The docstring said "the target itself flips to `0` once inside
     the horizon window"; the addendum's correct version is "near-constant `1` for the whole
     post-freeze window, flips to `0` only at the episode's very last step." Reworded
     `src/experiment/metrics.ts:14-17`'s docstring to match the addendum — no behavior change, the
     code already implemented the addendum's version (`worldModel.ts:184`'s `done ? 0 : 1`).
  2. **"restoring the recon+KL-only series the 114 existing manifests were measured on" overstated
     what PR #64 delivers.** `WorldModel`'s `trainableVars` (`src/model/worldModel.ts:135-139`)
     includes `continueHead.trainableWeights()` with no stop-gradient, so since the continue head
     landed, the shared trunk (`RSSMCell`) trains jointly on `recon + kl + continue` whenever
     `train` is true — `continueLoss` still shapes the cell's weights even though PR #64 excludes it
     from the series `postFreezeLossSeries` reads. #64 restored the series' *composition* (the
     recon+KL formula), not its *comparability* to pre-continue-head numbers — those are a
     different quantity now, formula match notwithstanding. Added a paragraph to
     `docs/explainers/0007-drift-attributable-error-metric.md`'s addendum making this explicit
     (no code change, per the reviewer note's own framing — "a sentence in the addendum").
  3. **The `worldModel.ts:184` `done ? 0 : 1` polarity has no invariant test pinning it** (PR #63's
     smaller item, resurfaced this run because my #64 "Next:" line only named the loss breakdown
     and let it drop). Not fixed this run — the human's comment frames it as folding into priority
     5's invariant tests, not a standalone fix — carried to "Tomorrow" below so it doesn't drop a
     second time.
  Full diff: `src/experiment/metrics.ts` (docstring only), `docs/explainers/0007-drift-attributable-error-metric.md`
  (new addendum paragraph), this report.

## Learned
- The shared-trunk gradient point (item 2 above) generalizes: any future per-head loss-breakdown
  split (not just recon/KL/continue) will have the same property as long as `trainableVars` mixes
  all heads' weights with no stop-gradient — excluding a term from a *reported* series never
  excludes it from *training*. Worth keeping in mind if a future metric wants a component that's
  both measured in isolation and trained in isolation; that needs an actual stop-gradient on the
  unwanted head(s), not just a narrower read of `EpisodeStepRecord`. `self_checked, high confidence`
  (confirmed by reading `worldModel.ts:135-139`'s `trainableVars` construction and `step()`'s
  `tf.variableGrads(forward, this.trainableVars)` call at line ~239 — `forward()` returns the summed
  `recon + kl + continue` scalar that `variableGrads` differentiates against the full list).
- `node_modules/` was not installed at all at this run's start (fresh container) — `npm install`
  pulled 125 packages cleanly and the tfjs-node native addon built and passed its smoke test
  (test 154, "tfjs-node loads its native binding and runs one op"), so this run has no 67/75-style
  caveat. `self_checked, high confidence` (ran `npm run typecheck:ratchet` and `npm test` after
  install, both below).

## Blockers
- none

## Decisions needed
- none

## Assumptions made (proceeding unless told otherwise)
- Treated PR #64's merge comment as an ordinary (non-hold) human reply under priority 1 — it answers
  a question and raises follow-ups but contains no "do not act on this now" / "wait until X" /
  "pick this up on the next scheduled run" language, so the 2026-08-29 hold-exception amendment
  doesn't apply and today's run processes it directly rather than deferring it.
- Item 2's addendum paragraph is written as informational scope-clarification, not as a proposal to
  change #64's fix — the reviewer note's own framing ("a sentence in the addendum, no code change")
  reads as already deciding this, so treated as an instruction to execute rather than a question to
  relay back.

## Tomorrow
- Priority 5 (vertical-slice hardening / invariant tests): fold in a test pinning
  `worldModel.ts:184`'s `done ? 0 : 1` polarity (target is `0` exactly when `done`, `1` otherwise)
  alongside the other invariants (KL free-bits floor, WM/AC gradient separation, NaN → graceful
  halt, continue/termination head) named in `loop/GOAL.md` priority 5.

## Manifest
- level: L2-P2-slice | increment: priority 1 (process PR #64 human reply) | files touched: 3 |
  runs: none
