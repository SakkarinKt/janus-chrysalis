# Stand-up — 2026-07-28 (run 29)

## Done

- Checked PRs first per `loop/GOAL.md` priority 1: PR #28 (2026-07-27 stand-up) merged with one
  human comment — the merge comment itself, posted right before merge, never previously processed
  since PR #28 is the latest PR. It confirms the tensor-leak fix and reproduces the pre/post
  numbers; adds a robustness note about `tf.tidy` and `tf.Variable`; backs the "2.3s total" timing
  reading with cross-commit numbers; **resolves** the 4.91e-4-vs-5.87e-4 discrepancy that
  2026-07-27's report left unresolved; and flags a non-blocking, pre-existing typecheck finding
  ("nothing typechecks this repo... 84 error lines... worth its own run"). Processing that comment
  **is** this run's increment.
- **Resolved the 4.91e-4 vs 5.87e-4 discrepancy.** The reviewer showed that dropping the
  fixed-weight overwrite (reverting to the PR #26-era random-init state) and re-running the same
  135-element sweep 4 times gives a `4.68e-4`–`8.38e-4` range, one run landing at `5.7967e-4` —
  matching their original `5.87e-4`. So `5.87e-4` came from a random-init run; `4.91e-4` (confirmed
  bit-identical across three separate processes) is the fixed-weight figure. The 2026-07-27 report
  called this "unresolved but no longer attributed to nondeterminism" — it's resolved now. Recording
  the correction here rather than editing that already-merged report in place, per this project's
  append-not-overwrite practice (e.g. PR #25's handling of the 2026-07-23 replay-ratio correction).
- **Documented, in code, that `tf.tidy` never disposes `tf.Variable`s** (even ones created inside
  it) — added one sentence to `checkFiniteDifference`'s docstring in `test/model/rssm.test.ts` so
  the invariant that makes the `tf.tidy` wrap safe against a future variable created lazily inside
  `forward()` is written down where the wrap lives, not only in a PR comment.
- **Gave the reviewer's "worth its own run" typecheck note its own run.** `npx tsc --noEmit` under
  this repo's own `tsconfig.json` produced 149 error lines (123 distinct `error TS` diagnostics) in
  this environment — not the reviewer's 84. Root cause: `typescript` is not a declared dependency
  anywhere in this repo (absent from `package.json` and `package-lock.json`); `tsc` resolved to a
  pre-existing global install already on this container's `PATH` (`/opt/node22/bin/tsc`, v6.0.2),
  not something `npm ci` provisioned. That global install's own `lib.dom.d.ts` throws two
  additional errors here (`TS6200`, `TS2687` — "conflict with those in another file") that look like
  an artifact of this container image, not of the repository, inflating the count past whatever the
  reviewer's own machine produced. **The 84-vs-149 gap is itself the finding**: typecheck output is
  currently a function of whichever machine happens to run it, not of the code, because nothing
  pins a `typescript` version. Did not add `typescript`/`@types/node` as devDependencies to fix
  this — a new dependency, needing its own approval per `loop/GOAL.md`, and out of scope for a
  PR-feedback-processing increment.
- `npm ci && npm test`: 47/47 passing, run twice for stability (2386 ms, 2817 ms — consistent with
  the pre-existing suite duration, no regression). No dependency changes, no training runs, no
  changes to `src/` (only a docstring comment in `test/model/rssm.test.ts`).

## Learned

- **[high, self_checked, execution-confirmed]** The 4.91e-4/5.87e-4 gap from PR #26 is resolved,
  not just ruled out as float32 nondeterminism: `4.91e-4` is the fixed-weight-test figure
  (reproducible bit-identically across three processes); `5.87e-4` came from a random-init run
  predating the fixed-weight conversion. Both stay well inside the `5e-3` tolerance either way —
  this only changes which number is the "real" one to cite, not the pass/fail conclusion.
- **[medium, self_checked, execution-confirmed]** `tsc --noEmit`'s error count for this repo is not
  currently reproducible across machines: this container's global `tsc` (v6.0.2, pre-installed, not
  repo-managed) reports 149 error lines vs. the reviewer's 84, and at least 2 of those (`TS6200`/
  `TS2687` in the global install's own `lib.dom.d.ts`) don't originate from this repo's code at all.
  Root cause: `typescript` isn't a declared dependency anywhere in the repo, so `npx tsc` (or a bare
  `tsc` on `PATH`) resolves to whatever happens to be installed on the machine running it.
- **[medium, self_checked]** Of my 149 sandbox error lines, 55 are inside `src/`/`test/`
  (mostly `TS2532`/`TS18048`/`TS2345`/`TS2322` from `noUncheckedIndexedAccess` strictness on
  array/object indexing, plus `TS2591` from a missing `@types/node` for `node:test`/
  `node:assert/strict`); the remaining ~94 are inside `@tensorflow/*`'s own shipped `.d.ts` files
  and the global TS install's `lib.dom.d.ts`. I can't state a real vs-parent delta the way the
  reviewer could on their own stable machine, since the count itself isn't stable in this
  environment — but nothing in this run's one-line comment-only diff would plausibly move any of
  the 55.

## Blockers

- none

## Decisions needed

- [ ] Carried forward, unchanged, still not blocking: the replay ratio for Arm-A training is
      unpinned (2026-07-24 stand-up; human noted "I'll pin it against the replay-buffer design, not
      now" — box stays `[ ]` per that comment).

## Assumptions made (proceeding unless told otherwise)

- Treated the merge-comment's several items (leak-fix verification, `tidy`/`Variable` note, timing
  corroboration, discrepancy resolution, typecheck follow-up) as one bounded increment, since all
  originate from the same unprocessed PR #28 comment — matching how 2026-07-27's run grouped PR
  #27's merge-comment items into one increment.
- Did not add `typescript`/`@types/node` as devDependencies to make `tsc --noEmit` reproducible or
  wire a typecheck script into `package.json`, even though that's the natural fix for today's
  finding — treated as a new-dependency decision requiring its own approval, not something to do
  unilaterally under a PR-feedback-processing increment.
- Read "worth its own run" as an invitation to actually run it and report what happened (including
  an inconclusive/environment-dependent result), not as an implicit ask to fix the 55 in-repo type
  errors it surfaced — fixing those wasn't requested and isn't on `loop/GOAL.md`'s priority list.

## Tomorrow

- Priority 3 (next up, no outstanding review follow-ups remain once this run resolves PR #28's
  comment): RSSM completion — world-model losses (KL balancing with a free-bits floor, observation
  reconstruction) and wiring `RSSMCell` into `src/experiment/freeze.ts`'s rollout, per `loop/GOAL.md`
  and every stand-up since 2026-07-22's stated next step. Explainer-before-implement applies to each
  new core piece.
- If the human wants typecheck made reproducible: pin `typescript` + `@types/node` as
  devDependencies (exact versions), add a `"typecheck": "tsc --noEmit"` script, and decide
  whether/how it gates CI — flagged as a candidate follow-up, not scheduled or requested.

## Manifest

- level: L2-P2-slice | increment: priority-1 (processing PR #28's merge-comment follow-up —
  4.91e-4/5.87e-4 discrepancy resolution, `tidy`/`Variable` doc note, typecheck non-reproducibility
  finding) | files touched: 2 (`test/model/rssm.test.ts` + this report) | runs: none (doc/test-
  comment-only change, no training run, no `manifest.json` scope)
