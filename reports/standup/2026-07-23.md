# Stand-up — 2026-07-23 (run 19)

## Done

- Checked `loop/GOAL.md` priority 1 first: PR #23 (2026-07-22 stand-up, merged) has one human
  comment, and it's already answered — the previous run's own follow-up comment on that same PR
  replied to it, landed the approved ADR-0002 addendum, and queued today's work as "tomorrow." No
  unanswered human replies or open "Decisions needed" items on the two most recent stand-up PRs
  (#22, #23) — moved to priority 2.
- **Priority 2, closing it out**: ADR-0002 decision 5's kill criterion has two halves — "end-to-end
  gradient correctness vs. finite differences" (resolved 2026-07-22, did not fire) and "usable
  steps/sec" (still an open number as of yesterday, since multi-step BPTT throughput couldn't be
  measured while it crashed). Measured it. Extended
  `experiments/2026-07-21-week3-stack-spike/benchmark.ts` with a multi-step gradient-throughput
  benchmark, sweeping truncated-BPTT chain lengths {2, 4, 8, 16, 32} at Arm-A dims (h=256, 8×4
  categorical latent, batch 16) on `tfjs-node` CPU. Ran it; wrote the results into the same
  `summary.json` next to the script (still not a training run — no environment steps, no seeds, no
  `manifest.json`, per the same exemption the existing forward/single-step numbers already used).
- Converted the raw gradient-steps/sec into proposal `0001`'s own unit (environment-steps/sec =
  chain length × gradient-steps/sec) and checked it against that proposal's "≤200K environment
  steps per arm per seed... laptop-CPU overnight run" budget: ≈45 minutes per arm/seed at any swept
  chain length — about an order of magnitude of headroom before "overnight" would be in question.
  **The steps/sec half of the kill criterion does not fire either.**
- Documented the result: `notes/adr-0002-js-ml-stack.md` §10 (new, dated entry — the full numbers,
  the env-steps/sec conversion, and the caveats), and a dated 2026-07-23 update in
  `docs/proposals/0001-direct-nonstationarity-measurement.md`'s status log. Did **not** edit
  `docs/adr/0002-js-ml-stack.md` (the formal ADR) itself — approval-required per `loop/GOAL.md`;
  proposed wording is in this PR's description for you to land at your discretion, mirroring how
  the gradient-correctness half was handled on 2026-07-22.
- `npm ci && npm test`: 47/47 passing, no code under `src/` or `test/` touched today — this
  increment only added a benchmark sweep and docs, so the count and pass rate are unchanged from
  PR #23. `npx markdownlint-cli2` on both changed docs files: 0 issues. No dependency changes, no
  training runs.

## Learned

- **[medium, self_checked, execution-confirmed]** Multi-step BPTT gradient throughput on this stack,
  measured for the first time: env-steps/sec stays roughly flat (~70–83/sec) across every chain
  length swept (2 through 32) — expected, since RSSM forward/backward compute is ~linear in chain
  length and per-call wrapper/dispatch overhead is a small, roughly constant fraction of it. Medium,
  not high, confidence: only 30 timed iterations per chain length (an order-of-magnitude read, not a
  precision benchmark), and this measures the RSSM forward/backward cost in isolation — no replay
  buffer, no real losses, no actual environment stepping yet.
- **[medium, self_checked]** The single-step gradient figure moved from ≈101 steps/sec (2026-07-21,
  a different container) to ≈57 steps/sec measured fresh in this run's container, with no changes to
  the forward/backward arithmetic on that path between those dates. Container-to-container CPU
  variance, not a code regression — flagged explicitly in both docs updates so the absolute numbers
  aren't read as a portable hardware spec.
- **[high, self_checked]** Both halves of ADR-0002 decision 5's hard kill criterion — gradient
  correctness (2026-07-22) and steps/sec (today) — evaluated and neither fired. The week-3
  stack-validation spike (`loop/GOAL.md` priority 2, opened 2026-07-21) is now fully closed out; no
  custom-autograd fallback is needed for the RSSM recurrence on this stack.

## Blockers

- none

## Decisions needed

- [ ] none this run — the kill criterion's remaining open question is resolved (did not fire); no
      new items raised.

## Assumptions made (proceeding unless told otherwise)

- Swept chain lengths {2, 4, 8, 16, 32} rather than one fixed length: no truncated-BPTT chunk length
  is specified anywhere in the repo (PLAN.html and proposal `0001` only fix the total ≤200K-env-steps
  budget, not how it's chunked into gradient steps), so the sweep stands in for "whatever chunk
  length training ends up using" rather than guessing one number. Happy to fix a real chunk length
  once that's decided, if you'd rather this benchmark target one number precisely.
- Used a smaller iteration count for the multi-step sweep (5 warmup / 30 timed per length) than the
  existing forward-rollout/single-step benchmarks (20/200) — each multi-step iteration's cost scales
  with chain length, so keeping the sweep's total wall-clock reasonable for a script that runs once
  per loop invocation seemed like the right tradeoff against measurement precision; the resulting
  numbers are explicitly flagged as order-of-magnitude, not precision, in both docs.
- Proposed the `docs/adr/0002-js-ml-stack.md` steps/sec addendum in this PR's description rather
  than drafting the ADR edit directly, matching how the gradient-correctness addendum was handled on
  2026-07-22 (approval-required for ADR edits beyond draft notes).

## Tomorrow

- Per `loop/GOAL.md` priority 3: RSSM completion — the world-model losses (KL balancing with a
  free-bits floor, observation reconstruction) — now that both the gradient-correctness and
  steps/sec halves of the stack-validation spike are closed out, this is next in priority order with
  nothing blocking it.

## Manifest

- level: L2-P2-slice | increment: priority 2 (week-3 stack-validation spike — multi-step BPTT
  steps/sec measurement, closing out the kill criterion) | files touched: 5 (4 modified + this
  report) | runs: none (throughput benchmark only, no training run — no `manifest.json` per
  `loop/GOAL.md`'s scope for non-training runs)
