# Stand-up — 2026-07-18 (run 14)

## Done

Checked PRs #7–#17 first per `loop/GOAL.md` priority 1. PR #17 (merged 2026-07-17) carries a
human review that **approves and merges with no changes requested**, but includes one concrete,
unprompted data point flagged as worth acting on: `@tensorflow/tfjs-node@4.22.0` (the exact
version pinned in `package.json` since PR #15) **fails to install on Apple Silicon** — a 404 on
the prebuilt `napi-v8/4.22.0` `darwin-arm64` binary, with no working source fallback, found by
the reviewer running it on real Apple Silicon hardware. The reviewer's own words: "the 'not yet
confirmed on darwin-arm64' line should stay — if anything it's now 'confirmed to break locally.'"
Processing that reply is this run's increment.

- `notes/adr-0002-js-ml-stack.md` §3: added a 2026-07-18 update upgrading the Apple Silicon risk
  from a "history of friction" (multiple past GitHub issues, community workarounds, no confirmed
  "just works" story) to **confirmed broken on the exact pinned version this repo depends on**.
  Attributed to the human's PR #17 review, not self-verified (this session's sandbox is x64
  Linux; there is no `darwin-arm64` environment available to independently reproduce the 404).
- `notes/adr-0002-js-ml-stack.md` §7: matching dated addendum in the decision-record log, same
  pattern as the 2026-07-14/07-15/07-17 entries.
- `docs/proposals/0001-direct-nonstationarity-measurement.md`: one-paragraph dated status update
  noting no new cell code landed this run and pointing at the ADR-0002 update.

Did **not** attempt a fix (pin bump, patch, or documented workaround) — no `darwin-arm64`
environment exists in this sandbox to develop or verify one against, and per `loop/GOAL.md`'s
dependency boundary, changing the pinned version is itself a dependency change needing its own
approval, not something to guess at blind. The reviewer's own framing ("worth a *future*
increment") reads as non-blocking for today, so this run stopped at documenting the finding
accurately rather than reaching for an unverifiable fix.

`npm ci && npm test`: 35/35 passing (fresh install in this sandbox; no source/test code touched
this run — docs-only change plus the stand-up report). `npx markdownlint-cli2` on both changed
files: 0 issues. No new dependencies, no training runs, no world-model-cell code this run.

## Learned

- **[high, human-reported, PR #17 review]** `@tensorflow/tfjs-node@4.22.0`'s Apple Silicon install
  problem is not just historical friction (per the older, self_checked GitHub-issue survey in
  ADR-0002 §3) — it is a confirmed, current 404 on the prebuilt `darwin-arm64` binary for the
  *exact version this repo pins*, with no working source-build fallback, verified by the human on
  real hardware. This is a materially stronger claim than anything this loop could self-check from
  its Linux x64 sandbox, and changes the risk register: it's now a known blocker for running this
  milestone's code on the human's own machine, not a "maybe, historically" caveat.
- **[medium, self_checked]** Re-ran `npm ci && npm test` fresh in this session (node_modules isn't
  persisted between sandbox runs) and confirmed 35/35 still passing on Linux x64, Node v22.22.2 —
  consistent with the 2026-07-15/07-17 findings that this stack is fine on this specific
  architecture; the Apple Silicon problem is architecture-specific, not a regression here.

## Blockers

- None for continuing work in this sandbox (Linux x64 unaffected). The Apple Silicon install
  failure blocks the human from running/verifying this milestone's code on their own machine if
  it's `darwin-arm64` — see "Decisions needed" below.

## Decisions needed

- [ ] The pinned `@tensorflow/tfjs-node@4.22.0` 404s on Apple Silicon with no source fallback
      (per your PR #17 review). This loop has no `darwin-arm64` environment to develop or verify a
      fix against. How do you want this handled: (a) you test a workaround/newer version on your
      own machine and report back what works, (b) the loop researches candidate fixes (e.g. a
      later patch release, a documented manual build workaround) via web search only, without
      being able to verify them, and proposes one in a future PR for you to test, or (c) deprioritize
      entirely since it doesn't block progress in this sandbox?

## Assumptions made (proceeding unless told otherwise)

- Read the reviewer's "worth a future increment" framing as explicitly non-blocking for *this*
  run — treated documenting the finding accurately in ADR-0002 as the correct scope for processing
  the reply, rather than reaching into fix-attempt territory (version bump research, patch
  authoring) that this run can't verify anyway.
- Did not touch `package.json`/the `4.22.0` pin itself — changing it would be a dependency
  decision, and `loop/GOAL.md`'s pre-approved carve-out only covers the *initial* addition at
  `4.22.0`, not a version change; treating any bump as needing fresh approval.

## Tomorrow

- If the human's reply on the Apple-Silicon question points to a specific next step, process that
  first per priority 1.
- Otherwise: the stochastic categorical latent sub-increment for the RSSM cell (prior/posterior
  heads, observation encoder, straight-through sampling via `tf.stopGradient`, finite-difference
  gradient-check test) — per the 2026-07-17 stand-up's "Tomorrow," likely needs further splitting
  into its own sub-increments given its size.

## Manifest

- level: L2-Arm-A | increment: 1 (processed PR #17 human review reply) | files touched: 3 | papers added: 0
