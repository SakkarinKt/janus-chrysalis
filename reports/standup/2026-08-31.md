# Stand-up — 2026-08-31 (run 1)

## Done
- Extracted `binomialUpperTail`/`binomialTwoSided` (duplicated identically in
  `experiments/2026-08-27-high-visibility-preregistered/run.ts` and
  `experiments/2026-08-27-power-aware-radius-analysis/analyze.ts`) into
  `src/experiment/statistics.ts`, with 10 new tests in
  `test/experiment/statistics.test.ts` (hand-computed binomial values against
  independently worked-out reference numbers, a symmetry-under-complement
  property, `k=0`/`k=n` edge cases, and a `p<=1` bound sweep). This module is
  inside `tsconfig.json`'s `include`, so it's now checked by `npm run
  typecheck` and `npm test` — until this run it was the only code computing
  the p-values published in `docs/proposals/0001` that CI never touched.
- Updated both experiment scripts to import from the shared module instead of
  redefining it. Verified behavior-preservation two ways: (1) full suite
  green, 146/146 (136 baseline + 10 new), `typecheck:ratchet` 44/44 exit 0 —
  unchanged from baseline, so the extraction added no new in-repo diagnostics
  in `src`/`test`; a `--skipLibCheck` spot-check of both changed
  `experiments/*.ts` files (outside `tsconfig.json`'s scope, so not covered
  by the ratchet) turned up only pre-existing, unrelated errors in
  `src/model/losses.ts`/`rssm.ts`, nothing introduced by this change.
  (2) Re-ran `analyze.ts` against its existing input artifacts and diffed the
  output CSV against the one already committed at
  `artifacts/2026-08-27-power-aware-radius-analysis/power-annotated-radius4-vs-radius6.summary.csv`
  — byte-identical.
- This was PR #60's unanswered review comment, processed under priority 1
  (an unanswered human reply on the most recent stand-up PR): "Next:
  `binomialUpperTail`/`binomialTwoSided`, still duplicated ... That is the
  other half of #58's Next and the half that matters."
- Per the same comment's explicit instruction, dropped the "Decisions
  needed" item carried forward from 2026-08-30's report — already answered
  by #58's review (re-read the priority list fresh against repo state every
  run; where wording and the tree disagree, the tree wins; striking done
  items from `loop/GOAL.md` is the human's amendment to write, not mine).

## Learned
- The two duplicated definitions were textually identical, including the
  inline comment on `binomialTwoSided`'s `lower` line, so this was a pure
  move rather than a rewrite-and-hope. Running both verification steps
  myself (test suite + a diff against committed output) rather than just
  reading the diff is what makes "behavior unchanged" a checked claim
  instead of an assumed one — the same bar PR #60's review applied to the
  telemetry extraction. confidence: high, self_checked.
- No literature research this run: priority 1 (an unanswered human reply)
  pre-empts the priority list per GOAL.md step 2, and this reply's ask was
  pure refactoring, not a research question.

## Blockers
- none

## Decisions needed
- none

## Assumptions made (proceeding unless told otherwise)
- Left PR #60's three non-blocking follow-ups untouched: the `dirty`
  (`git status --porcelain` emptiness) field for `captureRunEnvironment`,
  `writeTelemetry(dir, [])` writing a one-blank-line file instead of an
  empty one, and a `dir` missing its trailing separator silently writing to
  a sibling path. The review marked all three "NOT blocking — fold into the
  next telemetry touch," and this run didn't open `telemetry.ts`. Carrying
  them forward rather than treating them as this run's job.
- Placed the new module at `src/experiment/statistics.ts`, alongside
  `metrics.ts`/`telemetry.ts`, rather than a new top-level `src/stats/` —
  matches PR #58's review's own phrasing ("extract the run-telemetry and
  statistics helpers into src/") and the existing directory's shape.

## Tomorrow
- Per PR #58's review: the continue/termination head is next on the
  priority list, but it's gated on a research-design decision (`done` is
  horizon-only today; a real early-termination condition changes episode
  semantics and breaks comparability with every run already in
  `docs/proposals/0001`) that the review named as the human's call, not
  mine. Will raise it under "Decisions needed" with options rather than
  deciding it in an explainer, unless a new human reply on this PR
  pre-empts that first.
- Fold the `dirty` field and the two `writeTelemetry`/path nits into the
  next `telemetry.ts` touch, per #60's review.

## Manifest
- level: L2-P2-slice | increment: 1 (processing PR #60's unanswered review — binomial-statistics extraction) | files touched: 4 (2 modified, 2 new; report not counted) | runs: none (no new training/smoke run this increment — `analyze.ts` was re-run only for output-diff verification of already-collected data, matching that script's own "no new environment or world-model training runs" scope, so no new `manifest.json`)
