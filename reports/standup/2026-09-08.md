# Stand-up — 2026-09-08 (run 1)

## Done

- **Priority 1** (processing PR #67's review — see PR #68's merge comment, "#67's Next: line is
  the one open instruction; process it as a single priority-1 increment"): the seeded Arm-A
  re-run. `experiments/2026-09-08-seeded-arm-a-instrument-validation/run.ts` — fixes
  `2026-08-12-arm-a-instrument-validation/run.ts`'s `buildWorldModels()` (one shared, unseeded
  `config` object for both agents) by deriving one seed per agent index
  (`deriveSeed(seed, agentIndex)`), so the two Arm-A agents get reproducible but distinct initial
  weights instead of accidentally-different unseeded ones.
- New dated artifacts: `artifacts/2026-09-08-seeded-arm-a-instrument-validation/` (6 manifests +
  `results.summary.csv`; `telemetry.jsonl` written but not committed, per `.gitignore`).
  `artifacts/2026-08-12-arm-a-instrument-validation/` is untouched (`git diff --stat` against it
  is empty) — the review's own instruction, after PR #67's earlier cycle caught an overwrite of
  that exact directory breaking a citation in `docs/proposals/0001`.
- `weightInitDiagnostic` inverted as the review predicted: `sameSeedIdentical: true`,
  `differentSeedDiffers: true` (was `identical: false` in the unseeded 2026-08-12 baseline) — the
  pass condition, confirming `WorldModelConfig.seed` now determinizes per-agent weight init.
- `docs/proposals/0001-direct-nonstationarity-measurement.md`: appended a "2026-09-08 update"
  section with the run's table, the inverted diagnostic, and — importantly — a self-check that
  this is **not the first** seeded per-agent Arm-A-style run (see Learned).

## Learned

- **This exact per-agent `deriveSeed(seed, agentIndex)` fix already existed**, in
  `experiments/2026-08-13-paired-init-instrument-validation/run.ts`'s `buildWorldModels`, written
  up in `docs/proposals/0001`'s 2026-08-13 update — three and a half weeks before this run. I
  didn't invent a new scheme; I reused that one verbatim, self_checked high confidence (read the
  file directly). What still makes today's run non-redundant: `WorldModel` has changed materially
  since 2026-08-13 — `ContinueHead` (PR #63, its own seeded weights), the `step()` NaN-halt
  invariant (PR #58), and `driftAttributableError` excluding `continueLoss` (PR #64) all postdate
  that run. This is the first seeded Arm-A validation against the *current* `WorldModel`, not a
  replay — confirmed directly: today's `diffMean`s (`[−0.0938, +0.0046, +0.0000]`) are close to
  but not identical to 2026-08-13's (`[−0.0936, +0.0036, +0.0000]`) under an identical seeding
  scheme and identical `SEEDS`/config, consistent with the model having changed underneath.
  self_checked, medium confidence on the interpretation (the numbers themselves are direct
  output, high confidence).
- **Seed 1003's `diffMean: 0.0000` is a known artifact, not a new result** — verified directly
  against this run's telemetry (self_checked, high confidence): agent 1's actions differ from
  control on 23/38 post-freeze steps, but the frozen agent's own observation's
  partner-visibility component is `[0, 0, 0]` on all 38 of them (partner never within
  `viewRadius: 2` post-freeze this trajectory). This is the exact mechanism `docs/proposals/0001`'s
  2026-08-21 update already worked out (`postFreezeObservationDivergenceCount`) for the
  paired-init harness; today confirms it reproduces under Arm-A's no-sharing per-agent-seeded
  harness too, with a different concrete seed draw than either prior run. Recorded so a `diffMean`
  of exactly zero isn't mis-read as "the freeze mechanism does nothing."
- Gate (a)/(b) verdict is unchanged from 2026-08-13: still mixed-sign, still inconclusive. This
  run doesn't reopen the partner-visibility axis (closed 2026-08-27) or move any gate verdict —
  it only refreshes the milestone's own numbers under the current codebase, since the milestone's
  `arm-a-instrument-validation`-named script itself had never been re-run seeded.

## Blockers

- none

## Decisions needed

- [ ] none this run

## Assumptions made (proceeding unless told otherwise)

- Treated "the seeded Arm-A re-run" as producing a fresh, independent measurement against the
  *current* codebase (reusing 2026-08-13's proven seeding pattern) rather than a literal
  bit-for-bit replay of 2026-08-13's own numbers under new file naming. If a literal replay/
  citation-pointer to 2026-08-13 was what was actually wanted, say so and I'll fold this into a
  cross-reference instead of a new experiment directory next time.
- Diagnostic-only seeds (`4242`/`4343`) reused verbatim from 2026-08-13's
  `checkPairedInitDeterminism` rather than picked fresh, for direct comparability between the two
  diagnostics.
- Added the `docs/proposals/0001` update section as part of this increment (not a separate one) —
  every prior dated experiment run in this doc gets its own dated update section, so leaving this
  run out would itself be a "done-but-unrecorded" drift item of the kind PR #67's own review has
  caught before (e.g. the seeded re-run idea missing from a Tomorrow section).

## Tomorrow

- Priority 5's one remaining vertical-slice-hardening item: the WM/AC gradient-separation
  invariant test (still has no code to test against per 2026-09-07's stand-up — check whether an
  actor/critic ("AC") path has landed yet before attempting this).
- If the human wants a literal bit-for-bit reproduction of 2026-08-13's numbers instead of (or
  in addition to) today's fresh-seed run, that's a small follow-up: point
  `2026-09-08-seeded-arm-a-instrument-validation`'s `SEEDS`-loop `buildWorldModels` call at
  2026-08-13's exact same diagnostic seeds and note the two are intentionally identical.

## Manifest

- level: L2-P2-slice | increment: 1 (priority 1, processing PR #67's review) | files touched: 3
  (`experiments/2026-09-08-seeded-arm-a-instrument-validation/run.ts` new,
  `docs/proposals/0001-direct-nonstationarity-measurement.md` appended,
  `reports/standup/2026-09-08.md` new) + 7 new artifact files (6 manifests + 1 summary CSV) | runs:
  `artifacts/2026-09-08-seeded-arm-a-instrument-validation/` (6 manifests + `results.summary.csv`)
