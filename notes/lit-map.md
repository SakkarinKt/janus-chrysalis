# Literature map — world models in multi-agent RL

Status: 7 core papers. Target before gap analysis / proposal drafting: ≥10 core papers (per `loop/GOAL.md`).

**Tooling caveat (2026-07-04 run, still true 2026-07-05):** `WebFetch` returns HTTP 403 for every arXiv URL attempted, including a plain `https://example.com` control fetch on 2026-07-04 — an environment/proxy issue, not arXiv-specific blocking. Re-tested once on 2026-07-05 (still 403) and, per the human's reply on PR #1, this is being treated as a **persistent environment limitation for this loop** rather than re-tested every run — primary-source verification passes happen in the human's interactive sessions instead. All claims below were built from `WebSearch` result snippets, cross-checked across ≥2 independent sources per paper (arXiv listing + `arxiv.org/html` mirror + official GitHub/OpenReview/ACM-DL/proceedings-PDF/literature-review-site page), rather than a full-text primary read. Every claim is tagged `self_checked` per the co-authorship protocol, and confidence is deliberately capped at `medium` for anything past the abstract/framing level — see each paper note's "Verification note" for specifics. **Do not promote any of these to `verified` until a primary-source pass is done.**

## Design axis this map is organized around

The mission statement's central question is where a world model sits relative to the agents that co-learn in the same environment:

1. **Fully shared** — one world model, joint state/observations, all agents' policies condition on it.
2. **Per-agent + centralized aggregation** — separate world model per agent, but a mechanism (attention, mixing, shared upper-level latent) explicitly pools information to counter non-stationarity. → **MARIE**
3. **Per-agent + bi-level (train-time-only sharing)** — global info baked into an upper-level latent used only during training; execution uses a lower level that's already been informed by it, so no runtime sharing is needed at all. → **MABL**
4. **Per-agent + explicit peer-to-peer communication** — no central aggregator; agents exchange messages (GNN-style) that shape each other's world-model state directly. → **CoDreamer**
5. **Per-agent + comms via imagined rollouts, PPO-style policy update** — the original model-based-MARL reference point everything else is measured against. → **MAMBA**
6. *(Not yet mapped)* **Fully independent, no sharing at all** — the non-stationarity stress case / naive baseline every other paper implicitly argues against. Need to find or construct this as an ablation, not a citation — it's usually the strawman other papers compare to, rarely a paper in its own right.

## Papers (7/10)

| Key | Title | Year | Venue | Axis point | Note |
|---|---|---|---|---|---|
| `mamba-2022` | Scalable Multi-Agent Model-Based RL | 2022 | AAMAS | per-agent + comms, PPO policy update | [notes/papers/mamba-2022.md](papers/mamba-2022.md) |
| `mabl-2024` | MABL: Bi-Level Latent-Variable World Model | 2023/2024 | AAMAS | per-agent + train-time-only global latent | [notes/papers/mabl-2024.md](papers/mabl-2024.md) |
| `codreamer-2024` | CoDreamer: Communication-Based Decentralised World Models | 2024 | OpenReview / CoCoMARL ws. | per-agent + GNN peer comms | [notes/papers/codreamer-2024.md](papers/codreamer-2024.md) |
| `marie-2024` | Decentralized Transformers w/ Centralized Aggregation (MARIE) | 2024/2025 | TMLR | per-agent + centralized aggregation, explicit non-stationarity framing | [notes/papers/marie-2024.md](papers/marie-2024.md) |
| `gawm-2025` | GAWM: Global-Aware World Model for MARL | 2025 | arXiv | per-agent inputs + RSSM-based centralized aggregation, CTDE-preserving | [notes/papers/gawm-2025.md](papers/gawm-2025.md) |
| `matwm-2025` | Transformer World Model for Sample Efficient MARL (MATWM) | 2025 | arXiv | decentralized imagination + teammate-prediction module; non-stationarity via prioritized replay, not architecture | [notes/papers/matwm-2025.md](papers/matwm-2025.md) |
| `mmsa-2026` | Multi-Agent Model-Based RL w/ Joint State-Action Learned Embeddings (MMSA) | 2026 | arXiv (reported AAMAS 2026) | orthogonal axis — sharing via value-decomposition mixing network, not world-model topology | [notes/papers/mmsa-2026.md](papers/mmsa-2026.md) |

## Preliminary gap observations (self_checked, low–medium confidence — 7 papers, growing evidence but not yet ≥10; re-check again once the map is complete)

- **[low, self_checked]** All seven papers evaluate primarily on final-return / sample-efficiency curves. None of the abstracts/framings surfaced so far report a *direct, isolated* measurement of non-stationarity itself (e.g., world-model prediction error over training as a co-learning partner's policy shifts, holding everything else fixed). Holds up across all seven now — strengthening (not just preliminary) candidate gap: existing work treats non-stationarity as a problem to architect *around*, not a quantity to *measure*.
- **[low, self_checked]** No paper in the map runs the same architecture across a shared-vs-per-agent sweep on one fixed environment/task — each paper introduces one new architecture and compares it to prior papers' reported baselines (different codebases, possibly different hyperparameter budgets). A controlled, single-codebase sweep across the axis below, on one laptop-scale env, would be a novel (if modest) empirical contribution distinct from proposing a 6th (or 7th) architecture.
- **[medium, self_checked]** Non-stationarity mitigation now has *three* structurally distinct mechanisms in the map, not just "architecture choice": (1) architecture-level aggregation (MARIE, GAWM), (2) bi-level train-time-only latent sharing (MABL), (3) training-procedure-level recency-weighted replay (MATWM), independent of architecture. This suggests the controlled sweep's design space is at least two-dimensional — sharing *topology* × non-stationarity-mitigation *mechanism* — rather than one axis of competing architectures. MATWM's replay mechanism in particular looks factorizable: it could plausibly be layered onto any of the other architectures as an independent variable, which would make for a cheap, high-signal ablation.
- **[medium, self_checked]** MMSA (2026) doesn't fit the shared-vs-per-agent-*world-model* axis cleanly — its representation-sharing happens at the value-decomposition/mixing-network level, with the world model's own sharing topology unstated in the abstract. Flagged as a boundary case: pending a primary-source read to confirm whether MMSA's world model is per-agent or joint, since that answer decides whether it's a genuine 6th axis point or a contribution that's orthogonal to this map's organizing question entirely.
- **[low, self_checked]** Of the three papers added this session, only MATWM and GAWM name non-stationarity (of different flavors — agent-policy non-stationarity for MATWM, world-model-training instability for GAWM) as a first-class target; MMSA's stated motivation (data efficiency via richer joint embeddings) doesn't mention it. Combined with MAMBA/MABL/CoDreamer from the first batch, roughly half the map so far treats non-stationarity as a named target and half treats it as an implicit consequence of sharing design — worth quantifying precisely once the map hits 10.

## Not yet followed up (candidates for next lit-map increment)

- **"Dreaming of Others: Latent Teammate Modeling in World Models for MARL"**, arXiv 2605.31361 (May 2026) — surfaced by search as directly on-topic (factorizes RSSM latent into environment vs. teammate components, learns a Theory-of-Mind head, explicitly targets non-stationarity from partner policy shifts). **Still not added as a full paper note**: single-author listing and very recent date make it a higher hallucination/mis-citation risk than the others given search-only verification; the human's PR #1 reply confirmed holding it until a primary-source check is possible ("agree with holding it — add it only after a primary-source check; cautious epistemics over coverage"). Since WebFetch is a persistent limitation for this loop (not just today), this note stays open until either WebFetch access changes or the human does the check interactively.
- Drama: Mamba(-SSM, unrelated to MAMBA-MARL)-Enabled Model-Based RL, arXiv 2410.08893 — single-agent, but relevant to ADR-0002 (SSM vs. RSSM dynamics backbone).
- Need ≥3 more papers to hit the 10-paper target — next increment should prioritize the non-stationarity/opponent-modeling seed area (below) and/or citation-chase from MARIE, GAWM, and MATWM's own related-work sections once a primary-source read is possible.

## Non-stationarity / opponent-modeling-in-latent-space seed area

Growing: MARIE (architecture-level aggregation), GAWM (world-model-training-stability framing), and MATWM (training-procedure-level replay prioritization) now give three structurally distinct mechanisms, plus the still-unverified "Dreaming of Others" as a fourth (latent teammate modeling / Theory-of-Mind). Still this map's thinnest-evidenced area relative to the sharing-topology axis — continue prioritizing it for the next increment, especially citation-chasing from MATWM's related work (it explicitly compares itself against MARIE and likely cites other non-stationarity-focused MARL papers).
